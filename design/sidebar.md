# Sidebar

- Loading truth (ui15 job 13): every fetched sidebar surface composes the shared `Skeleton` into the content it is waiting for. Project, recent-conversation, and archive lists hold row shapes; the accounts drawer holds account and meter shapes; the Memory surface holds document-heading and prose-line shapes. Each gives way to data in place and inherits the primitive's ramped pulse and reduced-motion stop; loading copy and centered content spinners are not placeholders.
- Selection and hover performance (ui17 job 0): the shared `BounceIndicator` ignores list mutation/resize snaps while its roughly 300ms transform-only spring is traveling, so selecting a chat shows the entire arc in Projects and Chats. Row hover never lifts state into either list: each `MarqueeLabel` listens only to its nearest `data-marquee-hover` host and stops locally on leave. The app animation budget pauses offscreen row beams, shimmers, and pulses and pauses every CSS animation while the document is hidden.

App chrome
- Sidebar identity & activity (phase 5, on the beUI ai-sidebar anatomy; selection restored in ui16 job 0): desktop project and session rows are borderless `min-h-9 rounded-lg px-2` rows with NO filled hover or selected background — hover and selection are quiet ink shifts (`text-muted-foreground hover:text-foreground`, selected `text-foreground`), focus keeps the inset ring. Chat selection is the small primary `BounceIndicator`: each `ChatRow` carries its session id in `data-bounce-key`, and one dot follows the selected row on the beUI ramped spring path in both Projects and Chats. The unrelated new-chat/needs-attention orange dot remains gone. A project open in the workspace may still carry the static `data-slot="project-open-indicator"` mark because that denotes workspace layout, not chat selection. Project rows show the project icon tile, an overflow-aware `MarqueeLabel` (hover scrolls an overflowing name, fitting names never move; `flex-initial` so it hugs its text), the chat-count numeral inline at the title's end (`data-slot="project-session-count"`, never hover-revealed), hover actions, and a spring-rotated chevron. ui14 job 12 row laws: the hover actions are display-hidden at rest (`hidden group-hover/project:flex`, `touch:flex` on coarse pointers) so only the count and chevron hold fixed slots and the name takes the row's free width; a project row or chevron click is a pure dropdown toggle — it never selects the project or touches the workspace, a project opens only when one of its chats is clicked; project order is starred first, then last-opened-by-Willem (`project-last-opened-v1`, written on his open actions only), then name — session activity never reorders the list; a row's age reads from `sessions.updated_at`, which the Claude synchronizer derives from the transcript's last real user/assistant message — never file mtime, identity wiring, or a project move — so watcher cycles, label-pass marker lines, and watchdog touches cannot move a chat's age; and the hover marquee runs only while the pointer is on the row — on leave it stops and slides back to rest on `MarqueeLabel`'s 0.4s eased return, never a blink or a continued scroll. Session rows carry NO provider icon tile — just label, count badge, age, and the options menu. A chat row is the ONE shared `ChatRow` component at every viewport in both the Projects tab and the Chats tab (ui11 phase 15): identical padding, typography, chevron/dots trailing control, and shared `ChatRowMenu`; the only sanctioned differences are the Projects-tab nesting indent and the Chats-tab project-name subtitle lead. Session names are `text-[13px] leading-4` in BOTH tabs — never `text-sm`. The session block unfolds/folds with a 0.18s EASE_OUT height+opacity reveal, indented by depth padding (`pl-5`), no tree rule, with `space-y-1` breathing room and a margin under the New Session button. Project editing is the `ProjectEditDialog` (shared Dialog chrome, labeled Title / Path / Planner memory name fields; the path is editable — the rename API re-points the project row and its sessions).
- Project icons: `iconDataUrl` on the project payload (repo-root `app-icon.svg` → `icon.svg` → `icon.png` convention, else the bundled SpotOn icon from `public/project-icons/`, else null). `ProjectIcon` renders it at `h-4 w-4 rounded-[3px]` in the ai-sidebar `size-5` grid tile; the default is lucide Folder/FolderOpen — the beUI default, never a custom glyph.
- Activity shimmer: the vendored `BorderBeam`/`BorderBeamOverlay` (`src/shared/view/beui/BorderBeam.tsx`, npm `border-beam` engine) — rotate mode, mono colorway, ink `--beam-ink` (light `211 10% 42%`, dark `211 16% 78%`; a neutral silver, never the accent), ambient strength 0.4. A mid-turn chat row carries the beam (it replaced the old green pulse dot); a project row carries it while any of its sessions runs AND the project is collapsed — expanding hands the shimmer to the chat rows. "Runs" means planner and worker alike (dispatched, direct, maintenance): the count comes from the same enriched running-runs poll the counter drawers use, and the beam clears when all are idle. Worker-origin sessions (dispatched builds, verify stages, direct runs) never list as chat rows, so an expanded project row keeps the beam for them (`unlistedRunningCount`, codex job 5) instead of handing off to a row that does not exist; the poll itself reads a chain runner's live build and verify sessions off the chain registry (`listActiveDispatchRuns`), since the runner never passes through the in-server run registry. Mount beams through `useBeamPresence` so every appearance/disappearance is the engine's fade, never a hard cut.
- Footer activity stack (ui17 job 3): the expanded sidebar's live planner/worker activity is the compact monochrome button at the right edge of `sidebar-taskbar`, beside Settings, account, and Memory. Worker is Hammer + count and planner is Compass + count; both active kinds stack worker above planner inside the `h-9` taskbar control, one active kind sits vertically centered, and zero active kinds render nothing. The trigger has no green, semantic status fill, label text, separate border, divider, or response stroke; it uses the taskbar's muted/foreground selected and dimmed ink. Clicking anywhere in the stack toggles the combined active-sessions drawer. The collapsed rail keeps its compact counters. Planner = origin `planner` or null, worker = `direct`/`dispatch`/`external` — the same split as the pane headers.
- Footer taskbar and integrated drawers (ui13 job 4): the sidebar footer's stacked rows became one left-aligned row of icon-only controls (`data-slot="sidebar-taskbar"`, `px-2 py-1.5 gap-1`) — Settings, account (`AtSign`, `data-slot="account-switcher-trigger"`, `title` carries the active email), Memory (`BookMarked`, `data-slot="memory-viewer-trigger"`) — each a `touch-hit h-9 w-9` ghost icon button (44px hit area), no labels. The open drawer's icon reads selected (`bg-accent/60 text-foreground`); while any drawer is open the other icons dim to `text-muted-foreground/40`. The footer drawer shell (`SidebarFooterDrawer.tsx`) has no panel chrome on desktop: no portal, no `bg-popover`, no border/shadow — an in-flow region on the sidebar's own background that unfolds between the footer's divider and the taskbar by animating height 0↔auto (0.3s EASE_IN_OUT ramp both ways — a height collapse cannot leave an exit sliver), growing the footer so the lists above squish up while the taskbar stays put; a trailing `nav-divider` inside the animated region separates content from the taskbar. Escape closes in the shell; a press outside the footer closes via `SidebarFooter`'s document pointerdown listener; a second trigger tap toggles. Drawers and surfaces are mutually exclusive both ways (ui14 job 4): opening a drawer closes any surface (`onDrawerOpened`) and a surface opening (Settings, Memory) ramps the open drawer closed through the same height collapse. No divider bar sits on top of an open desktop drawer: the footer's divider above the drawer region fades out (0.3s opacity) while a drawer is open and the drawer's own top padding separates it from the list. Phone: a full-width `bg-background border-t rounded-t-lg` bottom sheet portaled over the `bg-background/60 backdrop-blur-sm` scrim, same ramped curve (0.32s EASE_IN_OUT, full `y: 100%` exit).
- Account switcher (ui8 phase 6; drawer in ui11 phase 5; integrated ui13 job 4; revamped ui14 job 4): the taskbar's AtSign icon opens `AccountsPanel` (`src/components/sidebar/view/subcomponents/AccountsPanel.tsx`, `data-slot="accounts-panel"`) on the footer drawer shell — never a centered popup. No heading and no subtitle (a cswap switch rewrites the shared credential store that running Claude sessions re-read on token refresh, so no one-line claim about running sessions is true — the panel says nothing). The list sits in `px-2 pt-3 pb-2` (top padding, no divider bar). One borderless `rounded-lg px-2 py-2.5` row per cswap account (`data-slot="account-row"`, the sidebar row anatomy): a `w-4` mono slot numeral, the name (`data-slot="account-name"`, `text-[13px] font-medium`, alias or email) in full, and the stacked usage meters under it indented `pl-6` to the name's left edge. The active row is the `bg-accent/40` wash with the name in `text-foreground`; other rows are transparent with the name muted — there is no dot. Row actions (use, hold/return, move up/down; `h-7 w-7` ghost buttons, `data-slot="account-actions"`) are `display: none` at rest on desktop so the name owns the row, and appear on `group-hover` / `group-focus-within` — the only time the name truncates with an ellipsis; touch shows them always with `touch-hit`. Meters (`data-slot="account-usage-bar"`): `w-9` uppercase micro label, the `h-1 rounded-sm bg-muted` track (fill `bg-primary` / `bg-amber-500` ≥70 / `bg-destructive` ≥90 as data encoding) taking the rest of the width, `w-7` percent, `w-10` cswap reset countdown (clock fallback); 5h, 7d, then per-model windows. Add account is the real flow, no token or API-key field anywhere: the `Add account` row (`data-slot="account-add-trigger"`) expands to a two-step block (`data-slot="account-add-steps"`, `bg-muted/30`) — `1 Log in` with a mono `claude /login` chip (`data-slot="account-add-login"`) and `2 Add to cswap` with a `cswap add` chip (`data-slot="account-add-add"`), each opening the shell window with that command (`env -u CLAUDE_CONFIG_DIR BROWSER=/usr/bin/true claude auth login` / `env -u CLAUDE_CONFIG_DIR cswap add`, run against the machine-global profile like every server-side cswap call; the no-op BROWSER keeps the server machine from opening its own browser, so the terminal's printed sign-in URL is the way in), plus a "Waiting for it" micro status and Cancel. While the block is open the panel re-reads `/api/accounts` every 5s and when a shell window closes, and folds the block away once a slot appears that was not in the list when the block opened or the active slot changed (`src/components/sidebar/utils/accountsAdd.ts`). Server: `server/modules/accounts` shells the machine's cswap with `--json` and `CLAUDE_CONFIG_DIR` stripped (list, status, switch, disable, enable, swap — no add route); a switch re-mirrors the default Keychain credential into a scoped instance's own service (the command-center-dev-start.sh mirror, now also post-switch). ChatGPT group (codex job 3): below the Add account row, an uppercase micro group header `ChatGPT` (`data-slot="account-group-chatgpt"`) then one `account-row` (`data-account="chatgpt"`, `data-state` ok / logged_out / stale) on the same row anatomy with the OpenAI mark in the numeral slot, the login email as the name, the plan name as the bordered micro tag (`data-slot="account-plan"`), and the 5h / 7d `account-usage-bar` meters under it (same countdown/clock format as the Claude meters, a dash when Codex reports no window of that size); under the meters the muted meta line (`data-slot="account-usage-meta"`): "updated Xs ago" ticking every 10s, or "no usage recorded yet" when no rollout carries a reading. No use / hold / move controls (one login, nothing switches it). Logged out or a token stale for 30 days: no meters, one line "Logged out." / "Login stale." plus the mono `codex login` fix (`data-slot="account-login-fix"`). Data: `/api/accounts` carries `chatgpt` (`{email, plan, state, usage: {fiveHour?, sevenDay?, readAt} | null}`) from `server/modules/providers/services/chatgpt-account.service.ts`: the email and plan decoded from the id token in `~/.codex/auth.json` (never the token), the meters from the newest `token_count` event's `rate_limits` across `~/.codex/sessions` (a rescan on every read, newest file first, stopping at files older than the reading in hand). Live: the Codex session synchronizer hands every changed rollout to the service, which pushes a `chatgpt_usage` frame when the reading is newer; the panel subscribes only while open, and nothing polls.
- Usage freshness and parking (ui15 job 7; supersedes the account switcher's earlier open-only Live sentence): every Claude row ends with the same muted `account-usage-meta` "updated X ago" line as ChatGPT, sourced from cswap's `usageFetchedAt`; staleness is never implicit. The account drawer subscribes to full `accounts_usage` frames while open: immediate REST read on open and a 15s cache cadence, with reconnect resubscription. The Accounts monitor's always-on minute cadence reads cswap's daemon cache, never adds provider usage calls; switches and limit events refresh it immediately. A runner spend cap writes only the established `~/forge-logs/cswap-parked/<slot>` marker (`YYYY-MM email`) and never changes cswap's enabled flag. The marked row says `parked until YYYY-MM-DD` in `data-slot="account-parked-meta"` and exposes one `account-unpark` undo control; unpark deletes only the marker. Automatic runner picks exclude current-month markers, while hold/return remains the only automatic-rotation control that changes cswap enablement.
- Shell window (`ProviderLoginModal`, `src/components/provider-auth/view/`, `data-slot="shell-window"`; restyled ui14 job 4): the one place a terminal opens over the app for a single command — onboarding logins and the account add steps. A `max-w-4xl h-3/4 rounded-lg border-border bg-background` window over the `bg-background/60 backdrop-blur-sm` scrim (the sheet scrim), full-screen on phones, `overlay-enter` + `popout-enter-center` ramps. Its header is the pane header anatomy (`PANE_HEADER_CLASS`, Terminal icon + `text-xs font-medium` title, an `h-7 w-7` ghost close at right); the body is the minimal `StandaloneShell` running the command as a plain shell in the server's cwd. The window stays open after the command exits so the output can be read; closing is the X (the account panel re-reads cswap's list on close).
- Activity drawer (ui17 job 3): the footer activity stack opens one `ActiveSessionsDrawer` on the shared in-flow footer drawer shell, with only the active Planner and Worker sections. Sessions remain grouped by project with uppercase micro headers and a project-less bucket last. Each `active-session-row` uses the sidebar's borderless `min-h-9 rounded-lg px-2` row anatomy, monochrome live-state dot, label, and muted uppercase state word; working may pulse but never turns green. Planner rows carry the session title. Worker rows carry `slug Job N - name` when the manifest supplies a real name, otherwise `slug Job N`; dash-only missing-value sentinels render nothing, so the drawer never shows a placeholder double dash. Tapping a row opens that session, including unloaded sessions. Phone uses the same full-width bottom sheet and blur scrim.
- Memory surface (ui12 phase 7; full-sidebar ui13 job 5; curated-first ui13 job 8; one view + prompt edits ui14 job 3; prompt fit ui15 job 4): the footer taskbar's BookMarked icon (`data-slot="memory-viewer-trigger"`) toggles `MemorySurface` (`data-slot="memory-surface"`) on the full-sidebar surface shell. One view only: the curated memory document — `planner/_global/GLOBALMEMORY.md` in the spoton-worker repo, the planner-maintained record of Willem himself — rendered as clean prose Markdown (`data-slot="memory-curated"`, no row chrome, no section labels) under a `text-sm font-medium` "Memory" header with the BookMarked icon. No tabs: the Internals view and the Project/Global toggle are gone with nothing replacing them (the technical layers are not a user surface). Editing is the prompt box at the bottom (`data-slot="memory-prompt"`, Claude.ai style): equal `py-3` breathing room above and below the shared `PromptInput` enclosure, with an autogrowing `PromptInputTextarea` (placeholder "Iterate memory...", `13px/20px` on desktop, `text-base` on phones) and an `h-7 w-7` `PromptInputSubmit` — the composer's control scale, never larger. Enter applies, Shift+Enter breaks a line. Apply runs `POST /api/memory/curated/edit {instruction}`: a one-off headless `claude -p` session on the server (`--no-session-persistence`, tools limited to Read/Edit/Write, cwd the memory repo — not a chat, not a planner session) applies the instruction to the document by the document's own header rules (add, update in place, rotate stale), then the server commits (`memory: <instruction>`) and pushes the memory repo and answers with the new content; the send button shows the spinner while it runs (textarea disabled) and a `Check` for 1.6s after, then the arrow again, and the document re-renders from the response with no reload. Repeatable. Errors and post-edit warnings ("Committed, but the push failed.") sit inline under the box in `text-xs text-destructive` (`data-slot="memory-prompt-note"`); "No change" in the muted cut when the session left the file alone. The surface's own edits never produce a transcript row (the memory service holds a sentinel claim on the file while the edit runs). Data: `GET /api/memory/curated` → `{content}`. Import hook: at memory-watcher init the server looks for `planner/_global/claude-ai-memory-export.md`; if present it renames it `.imported` first (live and dev share the repo) and runs the same one-off edit with an import instruction (keep only what applies to how Willem works and his coding projects, drop unrelated personal facts, merge under the right sections, dedupe), committing both; absent, nothing happens. Desktop keeps the full-sidebar surface and its established spacing. Phone uses the shared bottom-sheet language inside the visual-viewport-pinned sidebar: a scrim, rounded top edge, internally scrolling document body, and prompt bar pinned above the taskbar and software keyboard.
Hover title scan + tooltip law (ui13 job 3): every truncating chat-row and job-row title reveals itself with the beUI `MarqueeLabel` hover scan — `ChatRow` titles (both sidebar tabs) and the jobs view's job/task unit titles scroll their overflow while the row is mouse-hovered and rest clipped otherwise, exactly like project rows; the scan is fine-pointer only (mouse enter/leave — touch taps act before hover matters; on touch the full name is readable in the pane header once the chat opens). Tooltips (native `title` or the shared `Tooltip`) exist ONLY on icon-only controls whose purpose is not self-evident — toolbar icon buttons, status glyphs/badges, drag handles, the collapsed-rail counters and job rings, meters. Never put a tooltip on an element whose visible text already says what it is (rows, titles, truncated paths/values — text echo), and not on self-evident icon controls (close/dismiss X, search-clear X, send arrows, prev/next steppers beside an n/N readout, expand/collapse chevrons); those keep aria-labels only.

Job 10 sidebar indicators supersede the older alignment, beam, drawer-dismiss, and account-group details above; ui17 job 3 supersedes its footer-counter treatment. `ChatRow` uses `pl-3`, aligned with the nested New session button and giving titles 4px more room in both tabs. A completed response not yet opened uses `ResponseSignal`, never a dot: one short primary stroke means planner, paired emerald strokes mean worker, and both stack; it appears on the chat row and the collapsed project row, and opening that planner or worker transcript clears it. Project beams use the same identity: planner is a long primary comet, worker a shorter split emerald comet, and simultaneous work overlays both through separate `useBeamPresence` lifecycles.

The account drawer is the deliberate live-watching exception to outside dismissal: desktop click-away and the phone scrim leave it open; only its taskbar trigger, Escape, or opening another drawer/surface closes it.

Accounts are one provider-sectioned list. Claude and ChatGPT use the same provider-mark plus uppercase-name group header, the same `account-row`, meter indent and reset geometry, and the shared `account-plan` tag wherever plan data exists. Add account remains inside the Claude section. A missing ChatGPT 5-hour window renders the muted text “no 5-hour window on this plan” in the meter row instead of an unexplained dash; the open list never autofocuses or auto-collapses.

Watchdog wake target (ui15 job 18, presentation corrected in ui16 job 0): the shared `ChatRow` in both Projects and Chats carries a muted mono `wake` tag (`data-slot="watchdog-wake-target-mark"`) on the one chat that receives project-level watchdog wakes. It is display-hidden at rest and appears only while that row is hovered on desktop; touch users find the same `wake` tag beside the existing manual action in the shared row menu. No bell glyph, tooltip, fill, or row-height change accompanies it. The shared row menu offers “Receive watchdog wakes here” (`data-slot="chat-row-menu-wake-target"`); choosing it moves the mark immediately across every rendered copy of the row. Chain wakes remain attached to the dispatching chat's handoff lineage, so this manual target governs only dispatches without session context and project-level maintenance or self-test wakes.
