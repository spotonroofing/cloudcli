<!-- browser -->
<!-- verify: no -->
<!-- name: ChatGPT in the switcher -->
<!-- tasks: ChatGPT group with 5h and 7d meters | Meters refresh from new rollouts | Signed-out state | Accounts API parity -->
Execute Job 3 of PUNCHLIST_codex.md in this repo (the ChatGPT login in the account switcher with real usage). The goal and items are that job's section and checkboxes in PUNCHLIST_codex.md; read only that job's section plus the stack and whole-file rules blocks at the top, and the recent git log; trust repo state over this prompt when they disagree, and check each item off in the file as verified. Done when the job's done check passes against the dev instance.

Read the files your job names in PUNCHLIST_codex.md before editing. Never speculate about code you have not opened. Only change what is directly required. Do not add features, refactor unrelated code, or introduce abstractions beyond what the task requires; do the simplest thing that works well. Do not add error handling, fallbacks, or validation for scenarios that cannot happen: trust internal code and framework guarantees and validate only at system boundaries (user input, external APIs). Do not use feature flags or backwards-compatibility shims when the code can just change. A bug fix does not need surrounding cleanup. Write a general solution: never hard-code values or special-case the done check just to make it pass; the check verifies correctness, it does not define the solution. If the done check itself is wrong or infeasible, stop and say so rather than working around it. Lean on dependencies already in the project before adding new packages, and prefer established, well-maintained libraries over reimplementing common functionality. Before running a command that changes state (restarts, deletes, config edits), check that the evidence supports that specific action; a signal that matches a known failure can have a different cause.

When you have enough information to act, act. Choose an approach and commit to it, revisiting only if new information directly contradicts it. Do not re-derive facts already established, re-litigate settled decisions, or narrate options you will not pursue; if you are weighing a choice, give a recommendation, not an exhaustive survey. Where subtasks are independent, delegate to subagents and keep working while they run; intervene if a subagent goes off track or is missing relevant context. Before committing, verify the result against the job's done check with a fresh-context subagent running claude-sonnet-5 at medium effort (name that model and effort when you spawn it); a reviewer that did not produce the work beats self-review.

Run every check, build, command, and subagent to completion in the foreground before ending your turn. Dispatch any subagent in the foreground and read its result in this same turn; never end your turn while anything you started is still running, and never end it with a message saying you are waiting on something. Wait for it, read its result, and land the commit before the turn ends.

Keep an append-only run journal at /Users/spoton-worker/forge-logs/codexint/JOURNAL.md; create the folder if missing. One line per entry, HH:MM | phase or task | event | detail. Write an entry only at these moments: run start (the goal in a few words), each phase or major step start, each phase end (include the commit hash), any item that fails, blocks, or gets skipped (the error in one line and what you did about it), any external-service error or unexpected state, and run end. Log boundaries and exceptions, never narration: no per-file play-by-play, no success spam, no reasoning. Append each entry with a single shell command; never rewrite or reformat the file.

You are operating unattended; no one is watching and no one can answer a question mid-run. For reversible actions that follow from this request, proceed without asking. Pause only for the things a person genuinely must do: a login or 2FA, a destructive or irreversible action outside this request's scope, or input only Willem can provide. Before ending your turn, check your last paragraph: if it is a plan, a question, a list of next steps, or a promise about work not yet done, do that work now with tool calls; if you are waiting on a subagent or background task, wait for it and fold its result in rather than ending your turn on a status note. End only when the task is complete, or you are truly blocked. Before reporting progress or completion, check each claim against an actual tool result from this session; report only what you can point to evidence for, and if something is unverified, say so plainly. If tests fail, say so and include the output. When you hit an obstacle, never take a destructive shortcut around it: do not bypass safety checks, do not delete unfamiliar files that may be in-progress work, do not force-push. When done, ensure .gitignore excludes .env, commit, and push.

Record lessons in /Users/spoton-worker/Projects/spoton-worker/planner/cloudcli/lessons/ (this is the memory repo, not the repo you are working in): one lesson per file, a one-line summary as the first line; corrections and confirmed approaches alike, including why they mattered; never save what the repo or PROJECT.md already records; update an existing note rather than duplicating; delete notes that turn out wrong.

Your final message is the reader's first look at any of this work; write it as a re-grounding, not a continuation of your working thread. Open with the outcome: one plain sentence on what happened or what you found. Then the supporting detail, then the one or two things you need from them, each explained as if new. Drop the working shorthand: write complete sentences, spell out terms, no arrow chains, no labels you made up while working; when you mention files, commits, or flags, give each its own plain-language clause. If you have to choose between short and clear, choose clear.

When the done check passes, ensure .gitignore excludes .env, then commit and push. You have ample context; do not stop, summarize, or suggest a new session on account of context limits. After the final commit and push, append a short section for this job (what you did, any deviation from the plan, what you skipped, anything blocking) to /Users/spoton-worker/Projects/spoton-worker/planner/cloudcli/sessions/20260827-codexint-summary.md, creating the file if missing, then commit and push that file in the spoton-worker repo.
