/**
 * The exact planner messages Willem reported as mangled (2026-08-23, ui9 round):
 * two-digit ordered-list markers rendered ".0"/"!1." and bold-led bullets were
 * suspected of stray "!" artifacts. Kept verbatim from the live transcript
 * (session 7cc98655) as renderer regression fixtures.
 */
export const DRAFT_LIST_25 = "Intaken, nothing dispatched. Housekeeping first: the promote finished clean (live is coherent ui8 at 72c0569, rollback snapshot kept), and the label backfill got killed by the restart mid-run — I relaunched it detached so it survives this time.\n\nui9 draft list (25 items, holding until your ready):\n\n1. No dot on a closed project row — keep the regular subtle highlight there; dot is for chats only.\n2. The chat's dot disappears as the project collapses, not after — no lingering during the close animation.\n3. Worker \"finished\" tag lags behind when dragging the divider — pin it to the header.\n4. Sidebar scrollbar centered in its right gutter (2-3px shift); only the sidebar needs it.\n5. Chat transcript scrollbar runs the full pane height — never starts above or overlaps the composer, bottom means bottom, no whitespace break at full scroll.\n6. Fix the message re-animating bug (an already-sent message replayed its pop-in mid-response).\n7. Projects-tab chat rows copy the Chats-tab row anatomy exactly: relative time bottom-left under the title, same padding (minus the project name).\n8. Move-to-project becomes a downward drawer/dropdown anchored to the button, not a centered modal.\n9. For chats already in a project, that menu shows \"Remove from <project>\" with an X icon instead of \"no project/standalone.\"\n10. App-wide overlay consistency pass — every modal, popover, and menu on one system with one animation language (library choice is the worker's call between what we already use).\n11. Unified row actions everywhere: arrow (with its hover animation) as the default trailing control on all chat rows; it morphs to three-dots on hover; one identical menu: rename, move to project, copy session ID, archive, delete — archive and delete split into two items.\n12. Project ordering in the sidebar — your \"decide for me\" item, suggestion below.\n13. Projects/Chats/Archive tabs get the animated sliding selector; no instant jump.\n14. Archive tab never flashes a loading state — straight to content or \"no archived items.\"\n15. Stacked view: the row grabber can also drag a row to the left/right edge to convert to column view, not just reorder.\n16. Pasted-text chip redone Claude.ai-style: square, \"PASTED\" label, first-sentences preview, click opens a scrollable viewer — no more \"Pasted text.txt\".\n17. beautiful.dev adopted for the transcript view end to end, done carefully.\n18. Its loading state replaces our thinking indicator: counter + animation copied; the three loader animations each bound to a different status word (churning, thinking, ...) rotating by activity.\n19. Fake streaming: their streaming-text look for Claude Code's chunked responses; thinking with all its modes (steps, reasoning, search, coding); no follow-ups; keep copy, rerun, and sources.\n20. Edit-and-resend with response versioning: pencil next to copy on your messages, loads the text into the composer, silently resends (no duplicate bubble), old response swapped out, and a Claude.ai-style ‹ n › navigator to flip between response versions — frontend-faked but consistent with Claude Code sessions.\n21. Approval card merged: theirs + ours — free-text option, radio vs multi-select, no auto-send (pick then confirm), tooltips.\n22. Task rows as the live phase navigator in the worker window: phases done/remaining, expandable per-phase tasks, breathing while running, replaces the \"finished\" tag, works down to single prompts — populated by me at dispatch via a structured phase manifest.\n23. Prompt bar redo: input row keeps only plus (left, spaced), model selector + send (right); below the bar: handoff left (maybe slash), smaller context ring bottom-right — Claude-desktop-inspired, never a permission-mode control.\n24. planner.md: I populate the phase manifest for every dispatched run (pairs with 22).\n25. planner.md: every intake ends with a \"needs your call\" list — each fuzzy item with my suggestion under it, so you can approve wholesale. (Applying it as of now.)\n\nNeeds your call — my suggestion under each:\n\n- **\"beautiful.dev\" — same as beui.dev or a different library?** Suggestion: I treat it as beui.dev (the names you listed — streaming, thinking modes, task rows, approval, prompt bar — all match its registry); correct me if it's a different site.\n- **Project ordering (12):** most-recently-touched project auto-floats to the top, no manual reorder — matches how Chats sorts and there's nothing to maintain.\n- **Overlay system (10):** standardize on the shadcn primitives already in the app, with our motion tokens; beUI pieces render inside them.\n- **Response versioning mechanics (20):** versions live in our DB per session, transcript untouched — a resend is a fresh turn under the hood, the old response hidden not deleted, so Claude Code history stays truthful.\n- **Prompt bar bottom row (23):** handoff + slash bottom-left, context ring + nothing else bottom-right; everything infrequent stays out of the input row.\n\nStanding must-dos already queued for this round: dev/live build isolation and run persistence across restarts. Send the rest when ready.";

export const BOLD_LED_BULLETS = "The ui8 round is complete and verified: all 7 phases committed clean (fa90e1a through 72c0569), 33/33 punch-list items checked, the final sweep re-ran every phase's done check at desktop and phone viewports (13/13 pass), tests green, dev healthy, no stops.\n\nWhat's waiting for your eyeball on dev:\n\n- **Sidebar truth:** only your chats in the lists (dispatch runs live solely in the worker switcher, tagged at birth now), all 12 projects back (dev's DB was missing 10 rows — data, not code), real 3-6 word labels backfilled across 67 sessions, test chats purged.\n- **Drafts:** everything you type persists per chat on the server and syncs across devices within seconds; the localStorage leak that carried text between chats is gone.\n- **Sidebar redo:** bounce dot is the sole selection indicator (two latent dot bugs found and fixed — its resize observer had never attached), quiet hover, inline chat counts, proper edit dialog with a genuinely editable path, counters as the bottom two-column breathing bar that vanishes at zero, 4px trackless scrollbars.\n- **Themes:** Steel Blue default plus Graphite, Ink, Moss, Copper, Dune in Appearance; monochromatic icons everywhere (data inks kept); every pill in the app is now a rectangle.\n- **Workspace:** true 50/50 draggable divider in the normal view, drag a project from the sidebar into the view (side edges = columns, bottom = stacked, with snap guides), hover-X on sidebar rows to close, the odd toggle button deleted.\n- **Accounts:** sidebar footer shows the active account and opens the panel — real cswap data (5h/7d/Fable bars with resets), use/disable/reorder on hover, masked add-token flow, token never stored or logged; switching takes effect for new sessions immediately.\n\nHousekeeping: the six phase sessions this very chain leaked into your live sidebar (it ran on the old runner code) are already retagged by hand — your sidebar should be clean right now. At promote time the data-fix and label scripts get re-run once to catch anything the old live build indexed.\n\nRule audit: no violations observed.\n\nEyeball dev at :8443 when ready. On your promote word I'll run the promote plus the catch-up sweep — as the last action of the turn this time, so nothing gets severed. I'd also pull \"runs survive server restarts\" into the next round; it's flagged in STATE.md.";
