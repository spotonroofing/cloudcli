# MIGRATION.md — mini-migration run log

Machine: Mac mini M4 16GB, macOS 26.2 (25C56). Projects root: `/Users/spoton-worker/Projects` (capital P; filesystem case-insensitive). Node v24.13.0, npm 11.6.2, Claude Code updated 2.1.220 → 2.1.235.

## Phase 0 audit record (2026-08-18)

Removed:
- Orca completely: app (/Applications/Orca.app v1.4.182), running daemon + 3 orca-tcc-login shells + their stale MCP children, ~/.orca, ~/Library/Application Support/orca, com.stablyai.orca.plist pref, /usr/local/bin/orca CLI shim, installer dmg in Downloads. No Orca LaunchAgents existed. Verified zero orca-named files remain.
- Era LaunchAgents unloaded, plists archived to ~/forge-logs/mini-migration/removed-launchagents/ (repos/data untouched): com.spotonroof.indeed-porter, com.spotonroof.indeed-porter-watchdog (porter was in CHALLENGED safe mode since 2026-07-28), com.spoton.forge-worker (forge-propagator worker), com.spoton.leap-gateway, com.spoton.leap-monitor, com.spoton.leap-tunnel (hermes + cloudflared), com.spoton.proxyfeed-ai (Codex-drain daemon).

Kept:
- Spec keep-list: Tailscale, ccsync (daemon live, repo synced), cswap, Parsec.
- com.spoton.bw-session-refresh: required by this spec's own Bitwarden zero-touch requirement; verified `bw status` = unlocked non-interactively via ~/.local/state/bitwarden/session.env.
- LuLu (firewall) + Mos login items, Google keystone/updater (Chrome is required by spec §7), Docker daemons (on-demand, no boot activity; full Docker removal left for Willem's call).

Repos: all planner-mapped repos pulled (the brief's "five named project repos" is not enumerated anywhere on this machine; treated as the planner-roster superset: proxyfeed, spoton-worker, spoton-payroll, spoton-stats, spoton-core, spoton-book, SignTool, snapbridge-photos, acculynx-gateway, ghl, noggin, command-center — all present and current). spoton-core had 30 uncommitted staged changes blocking its pull; preserved verbatim on local branch `wip/pre-mini-migration-20260818`, main then fast-forwarded to 166e9bfd. Local-only scratch repo created at ~/Projects/scratch (git init, zero remotes). pmset: sleep 0, disksleep 0, autorestart 1, womp 1.

Test baseline (this machine): `npm test` server suite — **270 pass / 0 fail** (KEG's 5 env-dependent failures do not reproduce here). Full `npm run build` clean after fresh `npm install` (node_modules rebuilt from scratch).

## Phase log

- Phase 0 — environment and audit: complete. Orca removed, era agents archived, Bitwarden zero-touch + ccsync verified, repos updated, scratch created, fresh install + clean build, baseline 270/0, pmset set. Commit: 5e1305b
- Phase 1 — live service + tailnet HTTPS + backup: complete. launchd com.spoton.cloudcli-live on 4747 (RunAtLoad/KeepAlive, logs ~/forge-logs/cloudcli-service/), kickstart-restart verified clean; Tailscale Serve tailnet-only HTTPS at https://spoton-worker.tail6e1056.ts.net → 4747 and :8443 → 4748, /health verified over HTTPS (tailnet certs were already enabled — no admin-console pause needed); JWT session lifetime 7d → 90d (auto-refresh past half-life keeps devices signed in); nightly 03:30 backup job com.spoton.cloudcli-backup: 299M archive of ~/.claude + ~/.cloudcli created and tar-verified at ~/backups/cloudcli-nightly/, off-mini Taildrop delivery to silo/KEG attempted but the entire fleet is offline right now (silo last seen 18h ago) — the script delivers automatically the first night a target answers; first-delivery acceptance goes on WILLEM_CHECKLIST. Commit: f38e82b
- Phase 2 — dev/live isolation: complete. CLAUDE_CONFIG_DIR honored by watcher/synchronizer/skills/auth/mcp providers (getClaudeConfigDir helper); per-instance local-server marker via CLOUDCLI_INSTANCE (legacy filename kept for the Electron reader on other machines); dev service com.spoton.cloudcli-dev on-demand at 4748 with own ~/.cloudcli-dev/auth.db + ~/.claude-dev config dir; dev-start wrapper mirrors Keychain credentials into the CLI's config-dir-scoped service (Claude Code-credentials-fd736340, traced empirically) so cswap rotations reach dev at each start; single user registered on both instances (password in Bitwarden item "CloudCLI mini (live) — willem", never in a file). Isolation proven with real sessions both directions: live list shows only its session, dev list only its three. Commit: 4db4713
- Phase 3 — conversations restructure (B1): complete. App-owned assigned_project_path column (synchronizer never writes it; every list/feed reads COALESCE(assigned, cwd)); PATCH /sessions/:id/project attach/detach + scratch-project endpoint; global Conversations feed + Projects tab on desktop (mobile already had tabs); /standalone route hosts project-less chats in the hidden scratch repo (planner auto-boot suppressed there; scratch hidden from all project lists; session details present a __standalone__ pseudo project). Done-check passed in the live browser: standalone chat created from Conversations (Haiku replied), attached to proxyfeed via the move dialog, visible in both the feed (as proxyfeed) and proxyfeed's session list, and the assignment survived a full filesystem rescan (cwd stayed scratch, assignment intact). Fresh-context verifier: PASS. Commit: bb6f39e
- Phase 4 — worker pane + /worker (B2, B10): complete. sessions gain origin ('direct'/'dispatch') + base_commit; POST /sessions accepts origin, external /api/agent runs auto-tag dispatch with pre-run HEAD; GET /sessions/worker-latest + /sessions/:id/touched-files; WorkerPane auto-follows the latest worker session (poll + watcher deltas), New Session boots /worker with origin=direct, files-touched list opens files in the editor; desktop side-by-side split with edge toggle, mobile Worker tab; ~/.claude/commands/worker.md written (tiny B10 rules, ccsync propagates). Done-check in the live browser: pane session on Haiku 4.5 (non-default), origin=direct with correct base commit, appended a line to WORKER_TEST.md in cc-test-app and committed 6ad6945 'Add worker pane check entry for 2026-08-18' without pushing; pane followed it and the files link opened the file. Fresh-context verifier: PASS. Commit: 6f3e634
- Phase 5 — watchdog, scheduler, notifications (B3, B8): complete. Watchdog module inside the server: chain registry + boundary wakes (summary tail + verify instruction), serialized wake queue that retries while the planner is mid-turn and boots a fresh planner when the session is dead, 30-min stuck detection (assess, never kill), permission/interactive events on dispatched runs escalate straight to decision-needed, origin=direct never tracked, weekly push self-test, disk/memory threshold alerts (24h throttle); /api/watchdog routes (x-api-key) incl. POST /notify restricted to the two kinds; sendFleetNotification broadcasts to every stored push subscription + desktop clients with no preference filtering; foreground tabs play the self-hosted Orca two-tone (public/sounds/orca-notification.mp3, pulled from stablyai/orca) via a fleet_notification ws event; files/content route hardened with nosniff + inline Content-Disposition; main-surface sessions now tagged origin=planner so wakes target the right session. Done-checks: simulated chain completion produced queued+delivered wake in logs and a real planner turn that acknowledged; notify pipeline fired over all stored subscriptions (currently zero — device grants are Willem's checklist); browser tab attempted the Orca mp3 on the event (Audio hook proof); headers verified by curl. Fresh-context verifier: PASS. Commit: 7f0569b
- Phase 6 — dispatch primitive (B4): complete. scripts/macos/dispatch (zsh) installed on PATH via install.sh symlink: slug + ordered phase files (.dispatch/<slug>/ convention or explicit), fresh headless session per phase through POST /api/agent (origin=dispatch, chainSlug), commit gate stops the chain when a phase lands no commit, journals to ~/forge-logs/<slug>/, registers and reports to /api/watchdog; API key read from the server's own auth DB into a chmod-600 curl config (verified zero occurrences in logs/journals); Chrome-cap wait hook stubbed for phase 9. Done-check: toy 2-phase chain in scratch completed with both commits + journal boundaries; 3-phase chain stopped at the commit gate on the no-commit phase, watchdog got the stopped event and queued the wake. planner/reference/dispatch.md + PLANNER.md load rule committed and pushed in spoton-worker (d5529e6). Fresh-context verifier: PASS. Commit: b1fb7ae
- Phase 7 — promote/drain/rollback + planner rotation (B6, B7): complete. scripts/macos/promote.sh on PATH: full build, server suite, dev-instance health verify on the new build, drain of in-flight dispatched work (budget then decision-needed + abort), launchd restart, post-promote health check; last-good advances only after health (artifact snapshot .last-good/ + mini-last-good tag). Rollback restores the last-good ARTIFACTS instead of a literal git-checkout-and-rebuild — deliberate: it never touches the working tree (this build lives there uncommitted) and is faster, while the tag still records the good commit. Done-checks: real promote of the phase 7 tree succeeded through the full flow; forced-bad promote (live-only crash dev could not catch) failed health, rolled back automatically to a healthy live, and fired the decision-needed notification. B7 rotation: watchdog sweep computes used/total per tagged planner session, at threshold queues a /handoff wake then a fresh-boot wake and tags the new session planner; settings API + UI toggle/threshold (default 60, clamped 5-95). Proven live at a forced 5% threshold on cc-test-app: handoff filed and pushed, fresh planner booted from STATE.md and opened with the session-start summary (side effect: a real planner/cc-test-app memory dir now exists in spoton-worker — benign). Threshold reset to 60. Fresh-context verifier: PASS. Commit: c284b29
- Phase 8 — Monday self-maintenance (B9): complete. Weekly scheduler (Monday 09:05, five minutes after the push self-test) dispatches the maintenance run into this repo: upstream fetch + delta classification (backend-safe auto-apply through build/test/dev-verify/promote; frontend-touching gets one decision-needed notification; skip for release chores), Claude Code CLI version + release-notes assessment against the fork and doctrine files (safe silently with commits, judgment-shaped as decision-needed), nothing-to-do rows journal to ~/forge-logs/monday-maintenance/ with total silence toward Willem. Manual trigger POST /api/watchdog/maintenance/run (classifyOnly mode applies nothing). Done-check: classify-only trigger ran against current state and journaled correct classifications — upstream/main 677b7ba already an ancestor of HEAD, installed CLI 2.1.235 equals latest, silence correct (verifier confirmed both facts independently). Fresh-context verifier: PASS. Commit: 8ae372f
- Phase 9 — browser subsystem (spec 7): complete. agent-browser 0.31.1 present; CDP concurrency verified honestly: agent-browser DOES drive multiple CDP targets concurrently via named --session attach (two project Chromes on ports 9500/9501 driven simultaneously with isolated hosts + localStorage), so Playwright-over-CDP is NOT needed as the concurrent path. scripts/macos/project-chrome (fixed per-project debug ports in ~/browser-profiles/ports.json, persistent user-data-dirs, clone-from-seed only while seed is closed, --reclone for login failures, direct-launch cap refusal) + chrome-slot-wait (the chain-runner queue hook dispatch calls on browser-marked phases), both on PATH. Seed scaffold at ~/browser-profiles/_seed/ (no logins — Willem's checklist). ~/agent-auth/logins.json populated from Bitwarden (four domains: my.acculynx.com, app.enzy.co, accounts.intuit.com, accounts.google.com; flat domain schema; chmod 600; untracked — the home-dir git repo ignores everything by default, and neither path has tracked files). Done-checks: two simultaneous toy automations in separate profiles ran without touching each other's state; the fourth Chrome was refused at the cap and chrome-slot-wait demonstrably queued then released when a slot freed. Commit: 65c5921
- Phase 10 — skill rewrites + no-secrets rule (spec 8): complete except one Willem-gated verification. enzy-api-skill: hardcoded session token scrubbed from SKILL.md and the packaging allowlist; auth now resolves $ENZY_SESSION_KEY at runtime from Bitwarden (item 'ENZY_SESSION_KEY - enzy-api-skill - 2026-08-18', written back per the provisioning rule); verified with a real read-only GET /rest/leads/leadConfig through the documented resolution path; committed and pushed (ccsync auto-commit). acculynx-api skill: auth section rewritten to the full credential-provisioning chain (env → Railway → Bitwarden → browser-mint), no key material present; committed and pushed. Its real-call VERIFICATION is blocked at a sanctioned human step: the key exists nowhere reachable (no local .env, Railway CLI token expired, not in Bitwarden), and browser-minting at my.acculynx.com hit an emailed one-time code — the OTP exception only Willem can answer (reading the admin inbox is explicitly deferred by spec §12). Decision-needed notification fired; one-time seed-profile login goes on WILLEM_CHECKLIST, after which the mint + vault write-back is scripted into the skill's own auth section. Standing no-secrets rule added to PLANNER.md (f6900ad, pushed); dispatch.md and /worker already carried it. Commit: 2d58e3a
- Phase 11 — context repopulation + fleet boot (Phase C): complete. Fresh export (238 conversations + 26 claude.ai projects, in ~/Downloads since 19:31 today) unpacked and attributed; 19 PROJECT.md seeds regenerated by distill subagents scoped to preferences/hard-nos/things-tried/current-state/open-items (cloudcli seeded new from the ORCA project, tesla-hunt seeded new; books evaluated — its bundle held only qb-automations material, so its live seed stands; Personal/Legal/Nomad/Health excluded), pushed as spoton-worker 394a716. Fleet boot: 27 projects (every planner dir with a local repo) each booted a real Fable planner through the live run surface (origin=planner), absorbed its seed, initialized STATE.md with a current reconciliation anchor (verified all 27), and passed a smoke dispatch through the real chain runner (empty-commit phase on haiku; 34 chain-end wakes delivered back to the booted planners — the round trip). Two honest bumps, both fixed in place: spoton-core's pre-commit gate first failed on a missing pnpm (service PATH extended in the plists) then on its own stale-catalog freshness gate (regenerated via the repo's own script, committed 9c597f5a, smoke then passed as smoke-spoton-core-3); claude-sync's smoke commit was later absorbed by ccsync's history churn (journal records the pass). Boot journals in ~/forge-logs/smoke-*/. Commit: 1e3f6df
- Phase 12 — system self-test + Willem checklist: complete. Automated acceptance on proxyfeed, every step evidenced: a direct-worker session made an out-of-band commit (60d5611); the booted proxyfeed planner compiled a real two-phase punch list (8740a13), invoked the dispatch CLI once, both phases committed through the commit gate on haiku (a2880aa, 353c95e), the watchdog queued and delivered the chain-end wake, the planner verified against git log and the punch list, absorbed the direct commit via the git ledger, advanced the STATE.md anchor to 353c95e, and fired the verified-done notification through the notify endpoint (broadcast over all stored push subscriptions — zero devices enrolled until Willem's checklist; the pipeline itself is proven). Nothing was pushed from proxyfeed (Railway auto-deploy protection). WILLEM_CHECKLIST.md written: phone + desktop push grants, seed-profile login challenges (incl. the AccuLynx one-time code and follow-up key mint), Railway CLI re-login, backup first Taildrop delivery, OTP round-trip, promote eyeball, cutover call. Fresh-context verifier: PASS. Commit: (this commit)

## Run summary (2026-08-18)
All twelve phases of BUILD_mini-migration.md completed and verified in one unattended run. The mini now hosts CloudCLI live (4747, launchd, tailnet HTTPS) and dev (4748, isolated config + DB), with the conversations restructure, worker pane, watchdog + two-kind notifications, dispatch chain runner, promote/rollback + planner rotation, Monday self-maintenance, browser subsystem, runtime-resolved skill credentials, a remined 27-project planner fleet with reconciliation anchors and passed smoke dispatches, and an end-to-end proven acceptance loop. Open items are exactly the human gates in WILLEM_CHECKLIST.md, plus one build-side note: the AccuLynx skill's live-call verification waits on the one-time emailed code (everything else about that skill is done).
