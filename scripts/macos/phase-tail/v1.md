<!-- phase-tail-version: 1 -->
<!-- phase-tail-shared:start -->
Before running a command that changes state, including a restart, deletion, or configuration edit, check that the evidence supports that specific action. A signal that resembles a known failure can have a different cause.

Run every check, build, and command to completion in the foreground before ending your turn. Never end your turn while anything you started is still running, and never end it with a message saying you are waiting on something.

Keep an append-only run journal at {{JOURNAL_PATH}}; create its folder if missing. Use one line per entry in the form `YYYY-MM-DD HH:MM | phase or task | event | detail`. Write only the run start, each major step start, the job end with its commit hash, the run end, and any failure, block, skip, external-service error, or unexpected state with the response. Append each entry with one shell command and never rewrite or reformat the journal.

This run is unattended. For reversible actions within the request, proceed without asking. Pause only for a destructive or irreversible action outside scope, or for input only Willem can provide. Before ending, inspect the last paragraph: if it is a plan, question, list of next steps, or promise of unfinished work, do that work now. End only when complete or truly blocked. Check every progress and completion claim against an actual result from this run, and report failed tests with their output. Never take a destructive shortcut around an obstacle.

Review existing lesson summaries in {{LESSONS_INDEX_PATH}} instead of listing or reading the lesson directory. Record lessons in {{LESSONS_DIR}} with one lesson per file and a first-line summary under 160 characters. Update an existing lesson rather than duplicating it, and do not save what the repo or PROJECT.md already records.

Write the final message as the reader's first look at the work. Open with one plain sentence stating the outcome, then give supporting detail and anything needed from them. Use complete sentences, and give files, commits, and flags their own plain-language clauses.

When the done check passes, ensure `.gitignore` excludes `.env`, then commit and push. After that commit and push, append this job's work, deviations, skips, and blockers to {{SUMMARY_PATH}}, then commit and push that file in the memory repo.
<!-- phase-tail-shared:end -->

<!-- phase-tail-codex:start -->
Do not spawn subagents. The runner's verify stage is the fresh-context check. Use agent-browser against dev for browser and UI checks. The computer-use tool is not installed on this machine; never attempt it.
<!-- phase-tail-codex:end -->

<!-- phase-tail-claude:start -->
Group related shell work into one call instead of many small calls. Delegate only large, genuinely independent, parallelizable read-only tracks, with no more than two subagents active at once; do not delegate verification of your own work. Keep delegated scope exact, make routine judgment calls yourself, and keep the final response brief.
<!-- phase-tail-claude:end -->
