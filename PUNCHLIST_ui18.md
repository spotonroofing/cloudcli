# PUNCHLIST_ui18 — promote timing, the promoted divider, icon variations (2026-08-29)

## Goal

Willem's asks after the ui17 promote: a promote must let the running job finish instead of parking it mid-work; the jobs column shows when each promote landed; ten icon variations on the twisted-hoop mark for him to pick from. Dev-first, end-to-end verification, promote without an eyeball gate.

## Stack and decisions already made

- Verifier scope per doctrine: each job carries "Verify: yes" (the runner's verify stage) or "Verify: no" (Willem's eye is the reviewer).
- Standing laws: personal-tool wording, drawers not popups, ramped motion, monochromatic with semantic status color, DESIGN.md consistency, mobile parity, no em dashes in UI copy, label caps, rebuild discipline, pathspec commits for any planner files.

## Whole-file rules

- Read DESIGN.md before UI work; reuse the closest existing element; new elements get appended to the matching design area file.
- Ensure `.gitignore` excludes `.env` before any `git add`. Commit each job, push at job end, check items off in this file the moment each is verified, in the same commit as the work. Trust repo state over the phase prompt when they disagree.
- Progress honesty: claims check against tool results; UI verification via agent-browser DOM/snapshot on dev; confirm visible changes at a phone viewport.
- Run rules: browser and UI checks use agent-browser against dev (port 4748, config dir ~/.claude-dev, DB ~/.cloudcli-dev/auth.db); Codex's computer-use tool is not installed here, never attempt it; do not spawn subagents. Group related shell work into one call rather than many small ones, read files with the Read tool in the ranges you need, and do not narrate between tool calls. On unrecoverable failure stop and state what blocks.
- You have ample context; do not stop, summarize, or suggest a new session on account of context limits.

---

## Job 0: A promote lets the running job finish (2026-08-29, Willem: jobs should finish before that happens). Verify: yes

Goal: since ui17 Job 18, promote.sh pauses every running chain through `dispatch pause`, which stops the runner and the unit mid-work and parks the WIP; several "failed" jobs Willem saw were exactly that. A promote should hold at the next unit boundary instead: let the running build commit, let its verify settle, then pause there. Files: `scripts/macos/promote.sh` (the pause step), `scripts/macos/dispatch-chain-runner` (unit boundary, the reload point from ui17 Job 22), `scripts/macos/dispatch` (a `hold` subcommand or flag), the watchdog chain routes and records, tests, `design/worker-pane-and-jobs.md`.

- [x] Promote asks each running chain to hold rather than pausing it: a hold flag on the chain record (set through a dispatch subcommand the way pause is) that the runner reads at its unit boundary, after the commit gate and after that unit's verify has settled; the runner pauses itself there (same parked-clean state as `dispatch pause` on a clean tree, nothing parked mid-work), journals `HH:MM | run | HELD | promote`, and promote proceeds only once every chain reports held.
- [x] Bounded: a chain that has not reached its boundary within the drain budget (30 minutes, the existing `PROMOTE_DRAIN_BUDGET_S`) makes promote clear the hold, fire the decision-needed notice, and abort without touching live; nothing is ever parked mid-unit by a promote.
- [x] Resume is unchanged: after the post-promote health check (or a rollback) every held chain resumes from its next unit; the jobs column shows the chain as "holding for promote" between the boundary and the resume, using the existing paused treatment plus that label.
- [x] Every completed promote is recorded by the watchdog in one row (time, the promoted commit, the previous live commit, dry-run flag) in a `promotes` table, exposed on the watchdog routes, so the jobs column can draw it (Job 1); the record is written from promote.sh through the notify path, never by hand.

Done check: on dev with the stub chain mid-unit, a dry-run promote (ui17 Job 18's flag) waits for that unit's commit and verify, journals HELD, proceeds, and the chain resumes at the next unit with nothing parked; a stub unit that never reaches its boundary within a 20-second test budget makes the dry run abort with the notice and the chain keeps running; a completed dry run writes a promotes row; tests pass. Commit.

## Job 1: A promoted line in the jobs column (2026-08-29, Willem). Verify: no

Goal: Willem wants to see exactly when things went live: a thin divider between jobs in the history at the point a promote landed, with "Promoted" on the left and the date and time on the right, so he can tell which jobs are on live and which are not. Files: `src/components/worker-pane/JobsSidebar.tsx` (the history list and its period grouping), the watchdog promotes feed from Job 0, `design/worker-pane-and-jobs.md`.

- [x] A promote row: one hairline in the border color spanning the list width, a small mono muted "Promoted" label on the left and the promote's time on the right (the app's own time format for the row, a full date and time on hover), sitting between the last unit that landed before the promote and the first unit after it; it is not a job, has no ring, no chevron, no click, and never counts in any job total.
- [x] Rows come from the promotes feed live (a promote landing while the column is open inserts the row with the list's existing ramped entry motion); history pages that load later include theirs; before the first recorded promote nothing is drawn, no backfill and no guessing from tags.
- [x] Both column modes (side column and full-pane takeover), phone parity; `design/worker-pane-and-jobs.md` documents the row.

Done check: on dev with the stub chain and two stub promotes rows, the column shows two promote rows at the right positions with the label and time, no ring or chevron, and a third stub promote appearing live inserts a row with the entry motion; 390px holds; tests pass. Commit.

## Job 2: Ten icon variations on the twisted hoop (2026-08-29, Willem). Verify: no

Goal: the current Command Center mark (`public/mark.svg`, `favicon.svg`, the PNG sizes) reads fine but is not what Willem pictures. He wants ten variations to pick from, leaning abstract, floating on a transparent background. The seed image in his words: take a hula hoop, grab it by both sides and twist so the top and bottom lines cross; you get something like an infinity symbol where one lobe is heavier than the other. Files: new folder `assets/icon-variants/` (SVG sources plus one contact sheet), nothing in `public/` changes, no app wiring; `design/tokens.md` gains nothing until he picks.

- [x] Ten SVG variants at `assets/icon-variants/01-<name>.svg` to `10-<name>.svg`, each 512 by 512 with a transparent background, single monochrome ink that works on the app's light and dark surfaces (currentColor), built as clean paths (no raster, no filters, no gradients): four close to the twisted hoop (weight and lobe balance variations, open or closed stroke ends), three abstract derivatives (one continuous stroke with a gap, a partial hoop with the crossing implied, the two lobes as overlapping arcs), three on the nose for a command center (the hoop around a center point, an orbit with a core, a ring with a single bar), each with a one-line rationale in a `README.md` in the folder.
- [x] A contact sheet `assets/icon-variants/sheet.png` rendering all ten at 180px and 60px on both a light and a dark tile, so the phone reading is visible at a glance; note in the README which ones lose the crossing at 60px.
- [x] Show the work in your own chat, not in the app: after committing, write one markdown image line per variant (`![01 name](assets/icon-variants/01-name.svg)`) plus the sheet, then end with the plain list of filenames and rationales so Willem can name the one he wants. Nothing is wired into the app, the favicon, or the manifest.

Done check: ten SVGs and the sheet exist under `assets/icon-variants/`, each SVG parses and has no fill or stroke color other than currentColor, the README lists all ten with rationales, the images render inline in your chat. Commit.

## Job 3: Every chain carries its manifest, built from the phase headers (2026-08-29, Willem: jobs show as Job 1, 2, 3 with no tasks). Verify: yes

Goal: ui17r2 and ui18 were dispatched with phase files whose headers carry `name` and `tasks`, but without a `manifest.json`, so the watchdog stored no unit names or tasks and the jobs column renders "Job 1", "Job 2" with a plain circle instead of the segmented ring, no task rows, no punch-list anchor. The append path already parses headers; the dispatch path does not. A rule that lives only in the planner's doctrine ("every compiled chain should carry one") failed; the tool must carry it. Files: `scripts/macos/dispatch` (manifest resolution at dispatch time and the amend path), `server/modules/watchdog/watchdog.routes.ts` and `watchdog.service.ts` (chain registration, manifest storage, the amend route), tests, `design/worker-pane-and-jobs.md`.

- [ ] At dispatch time, when no manifest.json exists, `dispatch` builds the manifest from each phase file's headers exactly as `dispatch append` does (name, tasks, and the punch-list anchor derived from the "Execute Job N of PUNCHLIST_x.md" line so task counts tick), and refuses to start a chain in which any unit would have no name or no tasks, naming the file; a manifest.json, when present, still wins.
- [ ] A backfill route on the watchdog rebuilds a stored chain's manifest from its phase files on disk (the runner's resume.json lists them; appended units sit in the append folder), for running, finished and failed chains alike, safe against the in-memory registry; `dispatch remanifest <project> <slug>` calls it. Run it for ui17r2 and ui18 as the done check and leave the command for the planner.
- [ ] `dispatch amend` is allowed on the executing unit for name and tasks (the refusal stays for anchor changes on a unit already ticking), so a running chain's current unit can be repaired without waiting.
- [ ] Regression tests: a manifest-less dispatch on the stub chain produces named units with tasks and anchors; a dispatch with a unit missing `tasks` exits non-zero; remanifest restores names on a chain stored with numbered units.

Done check: on dev with the stub chain dispatched without a manifest, the jobs column shows named units with segmented rings and task rows; on live, after `dispatch remanifest`, the ui17r2 and ui18 rows read their real names (Handoff failure shows why, Promote owns the pause, Worker pane truth, Claude units out of Bash, Runner reloads, wakes land; Promote waits for the job, Promoted line in jobs, Icon variations) with rings and tasks; tests pass. Commit.

## Job 4: The jobs column loses its top bar, the worker pane names the active job, drawers follow the work (2026-08-29, Willem). Verify: no

Goal: the jobs column grew a header bar showing the chain slug ("ui18") and a second copy of the fast-mode control (the worker pane header already has one), which reads as clutter and says nothing Willem needs. He wants the bar gone, the worker pane title to name the job being worked on, and the job drawers to follow the work: when a unit starts, every open drawer closes and that unit's drawer opens so the active task is visible; when it finishes, its drawer closes and the next unit's opens; between boundaries he can open and close whatever he likes. Files: `src/components/worker-pane/JobsSidebar.tsx` (the column header, drawer open state), `WorkerPane.tsx` (pane title, the fast-mode control), `workerRunFollow.ts`, `design/worker-pane-and-jobs.md`.

- [ ] The jobs column has no top bar at all: no slug, no controls, no title; the list starts at the first row. The only fast-mode control is the one in the worker pane header (the bolt from Job 12); nothing else shows it. The chain slug appears only inside a unit's drawer, on its commit line, in muted mono.
- [ ] The worker pane title is the active unit's name while a chain is running ("Promote waits for the job"), and the last finished unit's name with the existing done treatment when nothing is running; the run switcher keeps its own labels.
- [ ] Drawers follow the work: at a unit start every open drawer closes (the existing ramped drawer motion, staggered so they do not snap together) and the starting unit's drawer opens; at a unit end that drawer closes and the next unit's opens as it starts; a manual open or close by Willem between boundaries is respected until the next boundary; one regression test drives start and end events through the stub and asserts the open set.
- [ ] Both column modes, phone parity; `design/worker-pane-and-jobs.md` documents the no-header column, the pane title rule and the drawer-follow rule.

Done check: on dev with the stub chain: no `[data-slot="jobs-sidebar-header"]` (or equivalent) element exists; exactly one fast-mode control exists in the whole worker pane DOM; the pane title equals the running unit's name; driving a unit start then end through the stub closes the open drawers, opens the active one, then moves to the next; 390px holds; tests pass. Commit.

## Job 5: One file card everywhere, and the planner can present a file (2026-08-29, Willem). Verify: no

Goal: files Willem sends in chat render differently from the composer's square attachment preview; he wants one card, the composer's square, for every attachment kind (image thumbnail, pasted text, any other file), same size, same corners, same label treatment. Separately the planner has no way to present a file to him except pasting its whole contents; an assistant message that links a local file should render the same card and open it. Files: the composer attachment preview component and the sent-message attachment renderer (`src/components/chat/`), the markdown renderer's link handling, a small file-serving route (assets already serve `~/.cloudcli/assets`; extend to files inside the project workspace and inside `~/Projects/spoton-worker`, read-only), `design/transcript-rows.md`, `design/composer.md`, CLAUDE.md's image section widened to files.

- [ ] Sent-message attachments use the composer's square preview card, one component shared by both places: a thumbnail for images, the first lines for pasted text, an icon plus name and size for anything else; identical dimensions and corners in every case; click opens the existing viewer (image zoom, text in a scrollable sheet, other files download); phone parity.
- [ ] An assistant message containing a markdown link whose target resolves to a file inside the project workspace or inside `~/Projects/spoton-worker` renders that link as the same card (name, size, kind) and opens it in the viewer with a download control; paths outside those roots, missing files, and remote URLs stay plain links. Markdown images keep their current inline rendering.
- [ ] CLAUDE.md's "Showing an image in the chat" section becomes "Showing a file or image in the chat" with the link form documented in two lines; a regression test renders a sent message with three attachment kinds and an assistant message with a local file link and asserts one card component for all four.

Done check: on dev with agent-browser, a message sent with an image, a pasted-text attachment and a PDF shows three cards of identical size; an assistant message linking `PUNCHLIST_ui18.md` and `~/Projects/spoton-worker/review-20260829/SYSTEM_OVERVIEW.md` shows two cards that open in the viewer with download; a link outside the roots is a plain link; 390px holds; tests pass. Commit.

## Job 6: The pane never traps its reader, second attempt with the real reproduction (2026-08-29, Willem, screenshot). Verify: yes

Goal: ui17 Job 19 landed two commits for this (b7a4d44 "a reader gesture releases follow before the repin", 2993c21 "a short pane refills and never traps its reader") and its verify passed, yet on the 4:15 am live build Willem hit it again at 12:50 pm: worker pane, a Codex session (ui18 unit 1, GPT-5.6 Sol) whose last row is the live "Thinking 10m 10.0s" indicator, the rows sit at the top of the pane, about 400px of empty pane below the indicator before the composer, the wheel does nothing, only a refresh recovers. The previous fix was verified against a Claude session with short tool rows; this state is a Codex session with a long-running thinking row. Two rules to land: the pane must never lock, and the live indicator row (Thinking, Percolating, a running tool row) always sits directly above the composer as the last row with nothing below it, in the same spot at all times. Files: the worker pane transcript scroller and its follow logic (`src/components/worker-pane/WorkerPane.tsx`, the message scroller hook, `workerRunFollow.ts`), the live indicator row and the bottom spacer, the Codex rollout reader (thinking rows for Codex), memory repo lessons follow-output-repin-needs-spacer-not-padding, message-scroller-guard-misreads-long-repins, bounded-tail-can-empty-a-pane, content-visibility-breaks-scroll-truth, `design/transcript-rows.md`, `design/worker-pane-and-jobs.md`.

- [ ] Reproduce first, with evidence: copy the live chain rows and the Codex rollout of ui18 unit 1 to dev (lesson copy-chain-rows-live-to-dev-by-named-columns), open the worker pane on that session at 1440 by 1000 and at 390px, drive a long live thinking row through the stub, and capture the DOM state that produces the gap (which element carries the height, its computed size, scrollTop, scrollHeight, clientHeight) before changing anything; the summary carries those numbers.
- [ ] Fix at the cause: whatever sizes the bottom spacer or padding can never exceed the space between the last row and the composer; the live indicator row is always the last row and sits flush above the composer; when content is shorter than the pane the rows anchor to the bottom (the indicator stays in its spot above the composer, empty space goes above the first row, not below the last); the scroll position is never locked, in any state, on either pane, for Claude and Codex sessions alike.
- [ ] Regression tests that cover this exact state, not a proxy: a Codex session with a live thinking row that has run for minutes, at two viewport heights, asserting no element below the indicator has height, that scrollTop moves on a wheel event, and that the indicator's bottom edge equals the composer's top edge minus the row gap; plus the earlier Claude short-row case stays green.
- [ ] Verify stage instruction: the verifier reproduces on dev with the same copied session before reading the tests; a verdict that did not open the pane on a Codex session mid-thinking is INCONCLUSIVE, not PASS.

Done check: on dev with the copied ui18 unit 1 session and a stub-driven live thinking row, at 1440 by 1000 and 390px: no element below the indicator has height, the indicator sits flush above the composer, wheel scrolling moves scrollTop, and the same holds after 60 more short rows stream in; tests pass. Commit.
