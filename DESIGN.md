# DESIGN.md — Command Center design system

Read this before any UI work. Reuse the closest existing element listed below; a new variant extends the existing pattern, never a parallel style.

One design across form factors: the desktop-only gating from UI rounds 1–3 is removed, so mobile renders the same language (radius token, silver-blue accent, unified composer with Handoff, grouped model switcher, usage ring + left-anchored popover, tool-row treatment, sidebar segmented control and bottom-pinned Settings, self-hosted font). Mobile-only functional controls with no desktop equivalent (tab rail, drawers, bottom sheets, keyboard insets, the Running view, create-project) stay, restyled with these same tokens — never a parallel style.

Mobile parity laws (ui7 phase 8): the mobile sidebar project/session rows are the same beUI ai-sidebar row anatomy as desktop (borderless `min-h-11 rounded-lg` rows — 44px-tall touch variant of the desktop `min-h-9` — icon tile, MarqueeLabel, count numeral, spring chevron), with actions always visible instead of hover-revealed; session actions open the bottom sheet (rename / move / copy / delete on token colorways). The mobile top bar is hamburger + tab rail only, on the pane-header bar chrome — the old chat-title block is gone on both form factors; titles live in the pane header. The sidebar refresh button is desktop-only; mobile refreshes by pulling the sidebar list down (`usePullToRefresh` + the indicator row in SidebarContent, `data-slot="pull-to-refresh"`). Touch laws on every mobile-reachable control: 44px minimum hit area — visually small controls keep their size and extend the tap area with the `touch-hit` utility (`src/index.css`, pointer-coarse only, requires `relative`); no action gated behind hover (fence hover reveals with `md:` or `touch:opacity-100`, never `sm:`); text inputs 16px below md (`text-base md:text-sm`) so iOS Safari never zooms on focus; bottom chrome pads `env(safe-area-inset-bottom)`; full-height mobile surfaces use `dvh`, never `vh`/`h-screen`.

## Brand

The app is Command Center (ui10 phase 2; mark redesigned ui11 phase 7). The mark is the command bracket — a caret held between two square brackets, the console frame around the prompt. Three round-capped, round-joined line paths in one stroke family, defined in `public/generate-icons.js`, which is the single source of truth: running it regenerates `public/mark.svg` (canonical, `stroke="currentColor"`), favicon.svg/png, the manifest icon PNGs, and `mark-{128,256,512}.png` (service-worker notifications + electron window icon). In React, render `CommandCenterMark` (`src/shared/view/CommandCenterMark.tsx`) — inline SVG, stroke inherits `currentColor`, so branded surfaces color it with a token class (`text-primary` on login and the auth loader) and it recolors with every theme. Never reintroduce a fixed-hex or filled logo; static renders (favicon, icon tiles) are the only baked colors and live only in generated files.

## Tokens

All color tokens are HSL triplets consumed as `hsl(var(--token))`, defined in `src/index.css` (`:root` = light, `.dark` = dark) and mapped to Tailwind utilities in `tailwind.config.js`.

### Typography

Self-hosted Schibsted Grotesk Variable (weights 400–900, normal + italic) via `@fontsource-variable/schibsted-grotesk`, imported at the top of `src/index.css`. No CDN font dependencies.

- `--font-ui` — UI chrome. Tailwind `font-sans` (the body default).
- `--font-message` — chat message content. Tailwind `font-serif` (legacy utility name kept so existing call sites resolve to the message cut; do not add real serifs).
- `--font-mono` — inline numbers, commands, code. Tailwind `font-mono`, resolving to the `'CloudCLI Mono'` @font-face in `src/index.css`: the local system monos wrapped with `ascent-override: 98% / descent-override: 26% / line-gap-override: 0%` — Schibsted's exact vertical metrics. Numbers sat off the line because the raw system monos carry smaller boxes (SF Mono 0.90/0.25), so flex-centering a mono span missed the sans baseline; with congruent metrics, centering IS baseline-sharing (ui11 phase 9). Never point `font-mono` at a raw system stack, and never per-site pixel-nudge a number — fix metrics here.

Both sans cuts currently point at the same family; keep them separate so the cuts can diverge.

### Radius

- `--radius: 0.5rem` (8px) — the one app-wide corner radius token. Tailwind `rounded-lg` = token, `rounded-md` = token − 2px, `rounded-sm` = token − 4px. Do not use `rounded-xl`/`rounded-2xl` on standard surfaces; desktop surfaces that carried hardcoded 12px radii were migrated to `rounded-lg`.
- Never apply asymmetric per-corner radii (`rounded-br-*` etc.) in the chat view. `rounded-full` is reserved for genuine circles (dots, spinners, radios, avatars, circular icon-only buttons); no pill/capsule shapes anywhere (ui8 phase 4) — every chip, badge, counter, status indicator, and thin bar track sits on the box-radius family (`rounded-md` for small chips, `rounded-sm` for tiny counters and bar tracks, `rounded-lg` for larger surfaces).

### Colors (semantic, light/dark via `.dark`, theme via `data-theme`)

`--background` / `--foreground`, `--card`, `--popover`, `--primary`, `--secondary`, `--muted`, `--accent`, `--destructive`, `--border`, `--input`, `--ring` — each with a `-foreground` pair where applicable. Nav-specific glass tokens (`--nav-glass-*`, `--nav-tab-*`, `--nav-input-*`) also live in `src/index.css`.

Named themes (ui8 phase 4): a theme is a full color-token set with light and dark variants, selected in Settings → Appearance (and the mobile quick-settings flyout), persisted in localStorage `color-theme`, applied as `data-theme` on `<html>` by `ThemeContext`. The catalog lives in `src/shared/themes.ts`; the base `:root`/`.dark` pair in `src/index.css` IS the default **Steel Blue** theme, and each additional theme (**Graphite**, **Ink**, **Moss**, **Copper**, **Dune**) is a `[data-theme='<id>']:not(.dark)` / `[data-theme='<id>'].dark` override block covering every color token plus `--beam-ink` and the nav tokens. Only color tokens vary per theme — radius, type, and spacing are shared. The browser `theme-color` meta follows the active theme's computed `--background`. New themes get a full token pair in both files; never a partial override.

Layered themes (ui10 phase 3): a theme is three independently designed layers — the surface ramp (`--background`/`--card`/`--secondary`/`--border`), the ink pair (`--foreground`/`--muted-foreground`), and the accent family (`--primary`/`--ring`/nav glow-focus) — so surface tint, ink temperature, and accent hue are deliberate separate choices (Ink is navy on warm paper with cool ink; Moss is green on cool stone with espresso ink; Copper is terracotta on cool porcelain with sepia ink; Dune is oasis teal on sand with brown ink). Never sweep one hue across every token. Each theme carries a palette preview in `src/shared/themes.ts` (`dots`: surface, secondary, ink, accent per mode) rendered by `ThemePaletteDots` inside both theme selectors' options — keep `dots` in lockstep with the CSS tokens when a theme changes.

Custom accent (ui10 phase 3): the Appearance tab's Accent row is a color swatch (`hsl(var(--primary))` background) wrapping a native color input, with a Reset text button when active. The picked `#rrggbb` persists in localStorage `custom-accent`; `ThemeContext` applies it as inline overrides of the accent token family on `<html>` — `--primary`, `--primary-foreground` (auto light/dark type by lightness), `--ring`, `--nav-tab-glow`, `--nav-tab-ring`, `--nav-input-focus-ring` — for both modes (dark lifts lightness to ≥62 so the accent reads on dark surfaces). It rides on top of whichever theme is active and recolors everything routed through `primary`/`ring`, including the mark; the selector dots swap their accent dot for the custom swatch. `--beam-ink` stays neutral and is never overridden.

- Accent: silver-leaning silver-blue. Light `--primary: 211 20% 45%` (`--primary-foreground` white); dark `--primary: 211 22% 64%` (`--primary-foreground` near-black, the lighter accent needs dark type). `--ring` and the nav glow/focus tokens carry the same hue per theme. Every accent use (send button, links, active states, selection ticks, progress ring fill, focus rings) goes through `primary`/`ring` utilities — never hardcode blue or orange Tailwind palette classes. Warning states use the amber family; nothing uses the orange family.
- User message bubble: `bg-secondary text-secondary-foreground` (lighter gray bubble on the darker background in dark mode, white text). No blue bubble, no avatar circle.
- Secondary/metadata text: `text-muted-foreground`.
- Icons are monochromatic app-wide in every theme (ui8 phase 4): control, menu, header, and empty-state glyphs render in `currentColor` / `text-muted-foreground` / `text-foreground` — never a decorative, identity, or destructive-intent color (no red trash cans, no per-language file-icon colors, no colored provider logos; `ClaudeLogo` is `fill-foreground`/`fill-background`). Destructive intent conveys through copy and confirm dialogs. Data inks stay data: diff add/remove, chart/legend colors, success/failure state glyphs, live status dots, task priority encoding, attention amber, recording red, terminal green.

### Shadow & spacing

- Bubbles and inline chat elements: flat, no shadow beyond what the component already carries.
- Popovers/menus: `border border-border bg-popover shadow-lg` with `rounded-md`/`rounded-lg`.
- Mobile safe-area and nav spacing variables (`--mobile-nav-*`, `--safe-area-inset-*`) are defined in `src/index.css`; use them, don't hardcode offsets.

## Reusable-element inventory

Shared primitives (shadcn-style, use these first): `src/shared/view/ui/`
- `Button.tsx`, `Input.tsx`, `Badge.tsx`, `Card.tsx`, `Alert.tsx`, `Dialog.tsx`, `Tooltip.tsx`, `Collapsible.tsx`, `ScrollArea.tsx`, `Command.tsx`, `ActionMenu.tsx`, `PillBar.tsx`, `PromptInput.tsx`, `Reasoning.tsx` (thinking accordion), `Shimmer.tsx`, `Confirmation.tsx` — exported through `src/shared/view/ui/index.ts`. (`Queue.tsx` was retired; task lists render through the beUI `TodoList`.) `ActionMenu` items take an optional `trailing` node — right-aligned `text-[10px] tabular-nums text-muted-foreground` metadata on the label line (the worker run switcher's relative date).
- `Badge.tsx` carries the beUI animated-badge skin — `rounded-md` rectangle (no pill, ui8 phase 4), `font-medium tabular-nums`, tinted `border-{color}/30 bg-{color}/10` colorways through semantic tokens (primary for default/info/loading, destructive for danger, emerald for success, amber for warning). Passing `status` opts into the animated state treatment: rolling icon/text swaps on status change, spinning loader with pulse for in-progress states; suppressed under prefers-reduced-motion. Wired on the worker/planner header state chips and the tool-row `ToolStatusBadge` — never invent status badges elsewhere.

Chat view: `src/components/chat/view/`
- `ChatInterface.tsx` — top-level chat screen.
- `subcomponents/MessageComponent.tsx` — one message (user bubble, assistant/tool/error layouts).
- Compact command bubble (ui11 phase 3, `data-slot="command-bubble"` in MessageComponent): any slash command sent from the composer renders inside the standard user bubble shell as its mono command name (`font-mono text-sm`) plus the command file's one-line description (`text-xs text-muted-foreground`, truncating); the expanded command text never shows by default — it sits behind a right-aligned rotating `ChevronDown` (`h-6 w-6`, `touch-hit`) that reveals a standard `rounded-lg bg-muted/80` mono viewport capped at 280px via `AgentDisclosure`. Commands travel as the Claude-style `<command-message>/<command-name>/<command-args>` wrapper plus body (`src/components/chat/utils/commandMessage.ts`, server twin `server/shared/command-message.ts`), so the bubble renders identically live and on reload. Command bubbles keep copy + timestamp meta but never the edit pencil.
- `subcomponents/MessageCopyControl.tsx` — single copy button, always copies plain text.
- Edit-and-resend versioning (ui9 B3): a pencil sits left of the copy control on text-only user turns (same hover-fade meta-row treatment); clicking loads the text into the composer and arms a silent resend — the composer shows a slim `data-slot="composer-edit-indicator"` row (`rounded-lg border-border/60 bg-muted/30`, pencil icon + copy, X cancels). A resent exchange renders `subcomponents/MessageVersionNavigator.tsx` (`data-slot="message-version-nav"`) under the visible response: always-visible left chevron / `n / m` tabular-nums / right chevron in `text-[11px] text-muted-foreground`, `touch-hit` on both buttons, arrows disable at the ends. Versions live in CloudCLI's `message_versions` table; the Claude transcript is never rewritten (hidden, never deleted).
- `subcomponents/MessageSpeakControl.tsx` — TTS button.
- `subcomponents/Markdown.tsx` — markdown renderer (`prose prose-sm font-serif dark:prose-invert` pattern). Fenced code renders in the beUI code-block chrome: `rounded-lg bg-muted/80` panel, `h-10` header (file icon, uppercase language tag, circular hover copy button with check swap), `border-t border-foreground/[0.06]` code viewport capped at 280px with internal scroll.
- `subcomponents/ChatComposer.tsx` — input composer, two-row Claude-desktop bar (ui9 B6). Input row (`data-slot="composer-input-row"`, `flex items-end`): plus/attach left, the autogrow textarea in the middle, model selector + send right; the flanking controls carry `mb-0.5` so they center on a one-line draft and stay pinned to the bottom line while a long draft stacks text above them. Slim secondary row (`data-slot="composer-secondary-row"`, ui11 phase 5: a sibling rendered BELOW the bordered enclosure, never a descendant of it — `mt-1 px-2`, `h-7 w-7` buttons, aligned to the enclosure's edges so it reads as floating under the bar): Handoff + slash (+ voice, + clear-input when non-empty) left; character counter + context ring right. Attachment previews (ui11 phase 5) sit inline above the text inside the enclosure (`data-slot="prompt-input-header"`, `flex flex-wrap gap-2`, no gray container): each preview is an 80px square carrying the enclosure's own border treatment (`rounded-lg border border-border/50 bg-background/80 shadow-sm`) — images `object-contain` (never cropped), non-image files an icon + truncated name + size square, pasted text the PASTED chip. No hint text, no permission selector — sessions are hardwired to skip-permissions. Focus state is a quiet gray (`focus-within:border-muted-foreground/40` + `ring-muted-foreground/20`), not the accent. The slash-commands icon is a custom single diagonal stroke matching the plus icon's arm length and stroke width; the Handoff button (document icon, `FileTextIcon`, all form factors, planner project chats only — hidden in the worker pane and standalone/scratch chats) fires the `/handoff` user command through `/api/commands/execute`. The character counter (`data-slot="char-counter"`, `text-[10px] tabular-nums text-muted-foreground`, digits rolling through `NumberTicker`) shows whenever the input is non-empty. A text paste over 2,000 characters collapses into a pasted-text attachment (delivered through the existing attachment path: `/api/assets/files` upload + `<files_input>` reference) whose chip is the Claude.ai-style square PASTED card (`data-slot="pasted-text-chip"` in `ComposerAttachment.tsx`: 80px square, miniature `text-[6px]` first-sentences preview, gradient-faded PASTED label bottom-left; clicking opens `data-slot="pasted-text-viewer"`, a shared-Dialog scrollable full-text viewer). Composer drafts are server-persisted per session (ui8 phase 2): text and uploaded-attachment descriptors save on a 500ms debounce to `/api/drafts/<key>` (key = session id, or `project:<projectId>` for the new-chat box), restore when the session opens anywhere, and sync live across devices through the `draft_updated` websocket broadcast; attachments upload the moment they are attached and their chips render from descriptors, so a draft's attachments survive reloads and device switches. `ComposerModelMenu.tsx` — Claude.ai-style model switcher (floating trigger: friendly name + effort + chevron in `text-muted-foreground`, no background fill or border — the send button is the input row's only blocky element; card + Effort and More models submenus; friendly labels only, wire values unchanged). More models hides the selected model (it renders as the checked card above) and splits the rest into current and legacy groups separated by a `ComposerMenuSeparator`, driven by the `group` field on the one plain catalog array in `server/modules/providers/list/claude/claude-models.provider.ts` (id, friendly name, tagline, group, in display order), `CommandMenu.tsx` — composer menus (portal + `bg-popover` pattern).
- `src/shared/view/beui/` — the vendored beUI kit (beui.dev), rethemed to this app's tokens: `MessageScroller` (follow-output transcript scroller — ResizeObserver keeps the live edge pinned, smooth-scrolling as streamed content grows), `StreamingResponse`/`useStreamedReveal` (live-turn reveal: word-boundary stepping at ~126 chars/s base plus a backlog-proportional catch-up rate so playback never lags real arrival — ui9 B2), `BrailleLoader` (horizontal ASCII Braille loader), `AgentDisclosure` (transform-only collapsible reveal), `SwapText` (roll label swap), `textShimmer` (the one text-shimmer treatment for in-progress labels), `ease.ts` (the motion springs/easings — use these, never ad-hoc curves). ui9 B2 additions vendored from beautifului.dev (www.beautifului.dev, MIT): `PixelLoader` (3×3 pixel-grid loading mark, variants drive/dots/orbit, keyframes `bui-pixel-on` in `src/index.css`, cells freeze dim under reduced motion) and `Thinking` (`data-slot="thinking-trace"`, the expandable agent trace — star-glyph header with shimmer active label / settled label / spring-free 300ms chevron, grid-rows collapse, left rule, fade-up rows — four modes: `steps` spinner→check step rows, `reasoning` prose, `search` query line + favicon source rows via `useFavicon`, `coding` mono rows with +/− counts). Phase-4 additions: `ActionSwapIcon` (blur icon swap for state-changing controls), `BeuiSwitch`, `BeuiTabs` (pill/underline/segment tabs with the `TABS_INDICATOR_SPRING` layoutId indicator), `BeuiSelect` (bouncy unfold select), `BeuiRadio` (`RadioGroup`/`RadioDot` gliding indicator), `NumberTicker` (rolling digit columns for live counts), `Loader` (dot-matrix + dither loading marks), `TodoList`, `Citations` + `useFavicon`, `ToolApproval`. Phase-5 additions: `BorderBeam`/`BorderBeamOverlay` + `useBeamPresence` (the activity shimmer — see App chrome), `MarqueeLabel` (overflow-aware row label from the ai-sidebar), `BounceIndicator` (bounce-sidebar active dot on a curved spring path). New collapsibles, controls, and loaders on the chat surface extend this kit.
- Control laws from the phase-4 adoption: any control whose icon changes with state renders it through `ActionSwapIcon`, never a hard cut (composer submit send/stop/queue, voice mic). Values that change while the user watches (composer character counter, activity-indicator elapsed time, context-popover header value) render through `NumberTicker` with a `format` callback — digits roll in place, unit glyphs (`k`, `M`, `%`, `m`, `s`) render as plain text, so the displayed string is byte-identical to the static version; inside the ticker every non-digit glyph (and prefix/suffix) gets the same fixed 1.1em box and matching line-height as the digit columns (ui9 B6), so digits and unit glyphs share one baseline instead of drifting with the inherited line-height. Every true loading overlay or pinned loading state (chat transcript, sidebar, code/PRD editor overlays, shell connection overlay, git panel views) uses the beUI `Loader` (`dot-matrix` app-wide, `dither` on shell/terminal surfaces); inline button spinners keep `Loader2`/spin. The composer model-switcher pill rolls its model/effort labels through `SwapText` on selection.
- `BeuiSwitch` is the one toggle: h-7 w-12 track `rounded-md`, h-5 w-5 thumb `rounded-sm` (box radius, not a pill), checked `bg-primary`, spring-driven thumb with press squish-stretch; optional `thumbContent` centers an icon in the thumb (DarkModeToggle's Moon/Sun). `SettingsToggle` and `DarkModeToggle` delegate to it.
- Tabs: `BeuiTabs` — a spring `layoutId` indicator glides between triggers. Underline variant (h-px `bg-primary`) drives GitViewTabs; segment variant (`bg-primary` pill on the rounded-md step) drives MainContentTabSwitcher inside the old muted bar chrome; the Settings desktop sidebar reuses the same spring for its gliding `bg-accent` row highlight. The sidebar segmented control (`PillBar`) is untouched pending the phase-5 sidebar steal.
- Selects: `BeuiSelect` replaces native `<select>`s — the panel bouncily unfolds out of the trigger and flips upward when out of room. Trigger `rounded-lg border-input bg-card` with `ring` focus; panel on the popover spec; selected tick `text-primary`. Homes: Appearance tab selects, LanguageSelector.
- Radio: `BeuiRadio`'s `RadioDot` — ring-and-dot whose inner `bg-primary` dot glides between options via shared `layoutId`. AskUserQuestionPanel's single-select rows compose it with `SPRING_PRESS` tap feedback; multi-select rows keep the checkmark.
- Context menus: the file-tree right-click menu unfolds with the beUI clip-path morph — the panel expands from an 8px-radius window at the cursor point (0.3s EASE_OUT), items use the spring shared-layout highlight instead of hover backgrounds, and danger items keep the rose colorway on the LABEL only — the icon renders neutral (icons are monochromatic app-wide, ui8 phase 4). Panel: `min-w-56 p-1.5 rounded-lg border-border bg-popover`; `data-slot="context-menu"`. Keyboard opens and reduced motion skip the morph.
- Tool-permission prompts: `PermissionRequestsBanner` renders pending requests through the vendored beUI tool-approval card (`ToolApproval.tsx`, `data-slot="tool-approval"`): a `rounded-lg border-border/60 bg-muted/20` card with a shield tile, "Allow this tool to run?" title over the mono tool name, an amber "Approval required" chip (rounded-md), the allow-rule code chip, and a "View tool input" reveal via `AgentDisclosure` into the standard `rounded-lg bg-muted/80` mono viewport. Action row: Allow once solid, Allow & remember outlined, Deny ghost, with `SPRING_PRESS` tap feedback. AskUserQuestion routes to `AskUserQuestionPanel` (`data-slot="ask-user-question"`), merged with the beautifului Approval Card in ui9 B2: `rounded-lg border-border bg-card` shell, kicker + header chip, quiet option rows with kbd hints (1–9, 0), `RadioDot` glide for single-select / `bg-primary` check square for multi, inline free-text "Other", and a footer with chevrons + ring-dot pager (multi-question) and a forward arrow-up button that advances and sends on the last — answers never auto-send; every icon control carries a shared `Tooltip`. ExitPlanMode stays with PlanDisplay.
- Task lists: `TodoList` (`data-slot="todo-list"`) — a bordered `rounded-lg` card with an `h-11` header (ListTodo icon that spring-swaps to an emerald check when all items complete, rolling `completed/total` count via `SwapText`, spring-rotated chevron) over an `AgentDisclosure` viewport capped at 248px. Rows carry animated SVG status circles: dashed pending, spinning arc in-progress, drawn check + strike-through completed. All TodoWrite/TodoRead tool rows render through it.
- Citations: `Citations` (`data-slot="citations"`) — a quiet "Sources" toggle revealing favicon rows (site favicon via `useFavicon` with a Globe fallback, title, domain, index chip) through `AgentDisclosure`. An assistant message whose markdown contains external http(s) links gets exactly one strip after the body, deduped by URL (`extractExternalLinks` in `chatFormatting.ts`); user messages, tool rows, and link-free messages never show it; inline prose links are untouched.
- `subcomponents/ActivityIndicator.tsx` — response-in-progress indicator (beautifului Loading State, ui9 B2): `PixelLoader` left of a shimmering status word plus a decisecond elapsed counter in mono tabular figures (`NumberTicker`). The word and its bound loader animation rotate together every ~6.5s of activity (Thinking→drive, Working→dots, Churning→orbit); server status text overrides the word. Transcript thinking blocks, WebSearch/WebFetch turns, and Task subagent turns render through the beUI `Thinking` trace (reasoning / search / steps modes; a subagent's result excerpt lives in the trace footer, so the separate Task result row is suppressed); consecutive same-tool groups keep the ToolGroupContainer anatomy with the coding-mode treatment — the header label shimmers while any tool in the run awaits its result. Live streamed turns wrap words in `.bui-stream-word` blur-in spans (`streamWords` on `Markdown`, skipped inside code/KaTeX) with a caret pseudo-element on the last block (`bui-stream-caret-host`). Settled assistant turns carry copy, speak, and rerun (resends the producing prompt through the composer submit path) — never follow-up prompts.
- Message rows: new live rows enter with the beUI message pop-up spring (`MESSAGE_POP_UP`, rise-and-scale from the bottom edge); loaded history renders statically. Message furniture (timestamps, copy, speak, edit pencil, rerun) follows ONE reveal rule on every row type (ui11 phase 4, `META_REVEAL_CLASS` in MessageComponent): `opacity-0 group-hover:opacity-100 touch:opacity-100` — hover-revealed on fine pointers, always visible on coarse; never the old `md:` width fence. The speak control stays visible mid-playback. User-message meta (copy + timestamp) sits below the bubble, outside it. A pasted-text attachment on a sent user bubble renders the composer's own PASTED chip and viewer (`PastedTextAttachment.tsx`, shared with `ComposerAttachment`), never a generic file card.
- `subcomponents/ChatMessagesPane.tsx` — scroll container (wraps the beUI `MessageScroller`); `TokenUsageSummary.tsx` — circular context-usage progress ring; click opens the context-usage popover (a "Context window" header row `<used>k / <limit> (<percent>)` with a chevron that expands the SDK-reported per-category breakdown; no billing/plan section). Ring and popover divide the same `used` by the same `total` (the SDK-reported window, persisted per model id in `app_config` when a live turn observes it and served on session load; env `CONTEXT_WINDOW` is only the never-observed fallback). The popover's left edge anchors to the ring button's left edge and opens upward on every form factor (`useComposerMenuAnchor` with `align: 'left'`; `maxWidth` shrinks it on narrow viewports). The ring lives at the right end of the composer's slim secondary row (ui9 B6): `h-7 w-7` button, 16px svg, unused track `stroke-muted-foreground/30` — never the border token, which matches the `hover:bg-accent` fill and made the track vanish on hover.
- `tools/ToolRenderer.tsx` (+ `tools/components/`) — tool input/result rendering. Every tool row (one-line, collapsible, Bash command row, grouped rows, subagent, error) carries the beUI tool-result treatment (beui.dev/components/agents/tool-result): a compact flush header row (muted icon or `$`, medium label, mono value in `text-muted-foreground`, status at right), a spring-rotated `ChevronDown` on the right when expandable, and content revealed through `AgentDisclosure` with a `pl-6` indent — expanded output/error bodies sit in a `rounded-lg bg-muted/80` viewport. Error rows use the rose status colorway (`text-rose-600 dark:text-rose-400`). No left rules, no per-category border colors.
- Tool-row trailing column (ui11 phase 4): every tool row header ends in a fixed `size-4` chevron slot (`grid size-4 place-items-center`, empty on non-expandable rows) so chevrons sit on one shared right-edge column regardless of tool name or content length; copy buttons are `size-5` grid buttons directly before the slot; the label column truncates. Tool rows never render the message meta line — a Bash row's timestamp sits right-aligned on its description line (hover-revealed, tabular-nums) and rows without a description carry none. Consecutive tool rows pack tight: `.chat-message.tool-row + .chat-message.tool-row { margin-top: 0.25rem }` in `src/index.css` overrides the transcript's `space-y` gap so a run of tool calls reads as one block.
- Interrupted marker (ui11 phase 4, `data-slot="interrupted-marker"`): a turn killed mid-response (stop button, server restart, process death) shows a small `Ban` icon + "Interrupted" row (`text-[11px] text-muted-foreground/80`) at the cut point. Sources: the CLI's `[Request interrupted...]` user row normalizes into a marker (reload), a live abort appends one on the `complete {aborted}` event (consecutive markers collapse in conversion), and a dead tool-call tail with no active run renders one in `ChatMessagesPane`.

App chrome
- Sidebar identity & activity (phase 5, on the beUI ai-sidebar anatomy; selection redone in ui8 phase 3): desktop project and session rows are borderless `min-h-9 rounded-lg px-2` rows with NO filled hover or selected background — hover and selection are quiet ink shifts (`text-muted-foreground hover:text-foreground`, selected `text-foreground`), focus keeps the inset ring. The bounce dot is the one honest indicator of the open chat: a 6px `bg-primary` dot (`BounceIndicator`) arcs on a curved spring path to the open session's row (`data-bounce-key`, stamped on the desktop and mobile row variants and on Chats-feed rows; the indicator measures the visible variant and re-seats through resize + DOM-mutation observers — its observers attach in a passive effect because a child's layout effects run before the host container's ref exists). When the dot's destination row isn't rendered — the open chat's project is collapsed, or a project is open as a workspace row — the project row carries the static `data-slot="project-open-indicator"` dot (`bg-primary/70`, same seat). Project rows show the project icon tile, an overflow-aware `MarqueeLabel` (hover scrolls an overflowing name, fitting names never move; `flex-initial` so it hugs its text), the chat-count numeral inline at the title's end (`data-slot="project-session-count"`, never hover-revealed), hover-revealed actions, and a spring-rotated chevron. Session rows carry NO provider icon tile — just label, count badge, age, and the options menu. Session names are `text-[13px] leading-4` in BOTH the Projects tab and the Chats tab — never `text-sm`. The session block unfolds/folds with a 0.18s EASE_OUT height+opacity reveal, indented by depth padding (`md:pl-5`), no tree rule, with `space-y-1` breathing room and a margin under the New Session button. Project editing is the `ProjectEditDialog` (shared Dialog chrome, labeled Title / Path / Planner memory name fields; the path is editable — the rename API re-points the project row and its sessions).
- Project icons: `iconDataUrl` on the project payload (repo-root `app-icon.svg` → `icon.svg` → `icon.png` convention, else the bundled SpotOn icon from `public/project-icons/`, else null). `ProjectIcon` renders it at `h-4 w-4 rounded-[3px]` in the ai-sidebar `size-5` grid tile; the default is lucide Folder/FolderOpen — the beUI default, never a custom glyph.
- Activity shimmer: the vendored `BorderBeam`/`BorderBeamOverlay` (`src/shared/view/beui/BorderBeam.tsx`, npm `border-beam` engine) — rotate mode, mono colorway, ink `--beam-ink` (light `211 10% 42%`, dark `211 16% 78%`; a neutral silver, never the accent), ambient strength 0.4. A mid-turn chat row carries the beam (it replaced the old green pulse dot); a project row carries it while any of its sessions runs AND the project is collapsed — expanding hands the shimmer to the chat rows. Mount beams through `useBeamPresence` so every appearance/disappearance is the engine's fade, never a hard cut.
- Active-session counters (ui8 phase 3): one full-width two-column bar (`data-slot="activity-counter-bar"`) pinned to the sidebar bottom above Settings — planner left (Compass + primary), worker right (Hammer + emerald), rectangular on the app radius (`rounded-lg border`, no pill). Counts roll through `NumberTicker`; a column breathes (`animate-counter-breathe`, opacity/filter swell, reduced-motion off) while its count is nonzero; the whole bar hides when both are zero. Each column is a button while its count is nonzero (ui10 phase 4): clicking jumps to the first running session of that kind (quiet `hover:bg-accent/60`, ring focus). The collapsed rail shows the same pair as `RailCounter` (icon left, number right, same breathing, same hide-at-zero) above the rail's Settings button. Planner = origin `planner` or null, worker = `direct`/`dispatch`/`external` — the same split as the pane headers.
- Sidebar: `src/components/sidebar/`; main layout: `src/components/main-content/`, `src/components/app/`. The main chat pane and the worker pane each carry the same slim header bar (`border-b border-border/60 bg-muted/30 px-3 py-1.5`, muted icon + `text-xs font-medium` label): "Planner" with the open session's title in `MainContent.tsx` (label follows the open session's origin — a worker-origin session shows "Worker" with the Hammer icon), "Worker" with the run switcher in `WorkerPane.tsx`. Both headers carry fail-safe chips through the shared `Badge` (`status="danger"`, `size="sm"` — rounded-md rectangle, destructive colorway): "disconnected" while the websocket is down, "stream mismatch" if the rendered transcript diverges from the claimed session for over 2s. On desktop each header ends in an `h-6 w-6` ghost X that collapses its pane into a slim `w-6` rail with a rotated `text-[10px] font-medium tracking-wide` label (planner leftward, worker rightward), persisted per pane in localStorage (`planner-pane-open`, `worker-pane-open`); the two headers measure the same height — the Tooltip wrapper is `inline-flex` so a wrapped button never adds phantom line-box height. New pane-level headers extend this bar, never a parallel style. `SidebarHeader.tsx` is one shared header (equal-width segmented control in the top button bar + ghost icon cluster + search; mobile adds the Running segment and create-project button, desktop the collapse button); `SidebarFooter.tsx` is the single bottom-pinned Settings row on both form factors.
- Multi-project workspace (phase 7, combine/close redone in ui8 phase 5): `WorkspaceView` (`src/components/app/workspace/`) wraps the desktop main surface. One open project renders MainContent with a draggable planner/worker divider (same slim-bar mechanics, weight-based flex, 50/50 default — the fraction is `workspace.split`, shared with rows mode). Projects combine by drag: every desktop sidebar project row is `draggable` (`PROJECT_DRAG_TYPE` dataTransfer); dragging it over the view offers edge drop zones — left/right edge opens the project as a column (`data-workspace-drop-zone`, columns mode), bottom edge as a stacked row — marked mid-drag by a `bg-primary/[0.07]` zone wash plus the standard 2px `bg-primary` snap guide. Each open project renders a `WorkspaceRow` — planner pane + worker pane on the standard pane-header bar; the planner header carries a leading drag grip, project icon + name, and trailing New-planner-session / layout-toggle ghost buttons; the row's close is the worker header's trailing X ("Close {name} row", the row's top-right corner), and the sidebar row of an open project shows a hover-revealed X (`data-workspace-close`) that closes it too. Rows mode shares one planner/worker split fraction so planners align over planners; columns mode (header toggle) lays the pairs side by side with per-project splits. Dividers are slim `bg-border/60 hover:bg-primary` bars; dragging a grip shows the 2px snap guide at the nearest drop boundary. Order, mode, split, and weights persist in localStorage `workspace-layout-v1`. The URL-driven selection is always the primary row; extra rows manage their own planner sessions (WorkerPane pattern) and never touch the app URL or the global selected session.
- Phase navigator (ui9 B4): `src/components/worker-pane/PhaseNavigator.tsx` (`data-slot="phase-navigator"`) — the worker pane's primary status surface, a full-width strip under the pane header on the TodoList row anatomy (`border-b border-border/60 bg-muted/20`, h-9 header). Collapsed: Milestone icon + "Phase N of M" (rolling `SwapText` ordinal) + current phase name + done/total counts + spring chevron; the icon+label cluster breathes (`animate-counter-breathe`) while the chain runs. Expanded (`AgentDisclosure`): every manifest unit lists at once as a scrollable stack of collapsible task drawers (ui11 phase 10) — each phase row is a drawer toggle (`data-drawer="open|closed"`, own spring chevron) whose `AgentDisclosure` reveals the task rows; the active phase's drawer opens by default, the others start collapsed, all toggleable (overrides reset on run switch). Phase rows carry the `TodoStatusIcon` state circles (dashed pending / spinning in-progress / drawn check completed / X cancelled); the in-progress ring advances with the phase's done/total counter (`progress` prop). Task rows (`data-slot="phase-navigator-task"`, `data-status`) carry a scaled `TodoStatusIcon` instead of a bullet — check for done, spinning working ring on the first unchecked task of the active phase, dashed idle for the rest (the done count maps onto the task list as a prefix, since punch lists check off in order); done tasks strike through. Appended "task" units render lighter (`min-h-7 text-[12px]`, indented, scaled icon) than full phases. New entries (manifest landing, `dispatch append`) stagger in one by one (70ms steps, EASE_OUT); settled rows never replay, and both header counts ("Phase N of M") roll via `SwapText` the moment an append lands. Each phase row carries a done/total counter on its right (`data-slot="phase-navigator-row-count"`, same `text-[11px] font-medium tabular-nums text-muted-foreground` + `SwapText` treatment as the header counts): total is the manifest's task count, done is read server-side from the run's punch list check-offs (manifest `punchlist` path + per-entry `anchor`); rows without countable tasks show none. The navigator defaults open on every run — switching runs resets a user collapse — and the pane header shows no status badge in any run state (ui11 phase 10; state lives here and in the run switcher). Data: the `worker-runs` response's `chains` snapshots plus live `chain_progress` websocket frames from the watchdog; a chain-less run renders as a single "Phase 1 of 1" row from its own state. Chain runs in the switcher are labeled "slug Phase N - name" from the manifest.
- Settings: `src/components/settings/`; quick settings: `src/components/quick-settings-panel/` (mobile-only edge flyout; desktop gets these settings in the Settings page's Appearance tab).
- Account switcher (ui8 phase 6; drawer in ui11 phase 5): the sidebar footer row above Settings (`data-slot="account-switcher-trigger"`, AtSign icon + the active Claude account's email) opens `AccountsPanel` (`src/components/sidebar/view/subcomponents/AccountsPanel.tsx`, `data-slot="accounts-panel"`) as a sliding drawer, never a centered popup: on desktop a sidebar-width panel (popover spec: `rounded-lg border border-border bg-popover shadow-lg`) portaled to the body and anchored over the sidebar with its bottom edge just above the accounts/settings block (measured from an anchor ref — the sidebar's backdrop-blur makes it the containing block for in-tree `fixed`, so the portal is mandatory), sliding up 0.22s EASE_OUT inside an overflow-hidden window and reverse-sliding closed via AnimatePresence; on phone a full-width bottom sheet over a `bg-background/60 backdrop-blur-sm` scrim. A second tap on the trigger, Escape, or an outside tap closes it (the transparent full-viewport backdrop catches outside taps). This drawer is the pattern phase 12's counter drawers reuse. One `rounded-lg border-border/60` row per cswap account: slot numeral, 6px `bg-primary` active dot (genuine circle), email, and stacked usage meters (`data-slot="account-usage-bar"` — `h-1 rounded-sm bg-muted` track, fill `bg-primary` / `bg-amber-500` ≥70 / `bg-destructive` ≥90 as data encoding, percent + cswap reset countdown at right). Row actions (use, disable/enable, move up/down) are `h-7 w-7` ghost buttons hover-revealed on desktop (`md:opacity-0 md:group-hover:opacity-100`), always visible on touch with `touch-hit`. The add-account row expands to a masked token input (`type="password"`) + optional email on the standard Input; the token travels only in the POST body to `/api/accounts/add` and cswap's stdin. Server: `server/modules/accounts` shells the machine's cswap with `--json` and `CLAUDE_CONFIG_DIR` stripped; a switch re-mirrors the default Keychain credential into a scoped instance's own service (the cloudcli-dev-start.sh mirror, now also post-switch).

Global CSS (scrollbars, transitions, chat containment classes, placeholder colors): `src/index.css`. Scrollbars are one app-wide treatment (ui8 phase 3): thin and trackless — 4px webkit width, transparent track, `muted-foreground/0.35` thumb on 2px radius, `scrollbar-width: thin` everywhere; never style a per-surface scrollbar.
