# PUNCHLIST_codex — Codex and ChatGPT as a peer of Claude

## Goal

Willem upgraded the mini's ChatGPT login to Pro. Codex CLI (GPT-5.6 Sol builds, Luna verifies, Terra is the manual weekly-cap fallback) becomes a first-class engine next to Claude everywhere the app already knows about Claude: the dispatch runner, the watchdog, the account switcher, the model switcher, settings, and the planner and worker boot paths. The server model catalog already carries Sol, Terra and Luna; Codex already works as an interactive provider. This round closes the gaps around them. Dev-first (port 4748), end-to-end verification, no promote in this round (the planner promotes after the ui15 rerun).

## Stack and decisions already made

- Fable 5 is the planner's model only, effort medium by default and sticky to Willem's last pick. Codex Sol at high (xhigh for hard jobs) owns backend, wiring and harness jobs; Opus 5 at high owns UI jobs; Luna always verifies; Fable never runs a dispatched job or a verify stage unless Willem names it (this round is that exception).
- Nothing automated may enable, disable or switch an account; the ChatGPT login is one account with no switch controls.
- Codex usage comes from Codex's own rollout files (the `rate_limits` block on `token_count` events under `~/.codex/sessions`); there is no cswap integration and none is wanted. Never invent a percentage: when no reading exists, say so.
- The tree carries uncommitted work in progress from ui15 job 0 (DESIGN.md as an index plus `design/` area files, the System settings tab, settings service and routes, `PlannerRotationSection.tsx` removed). Job 1 owns it: keep it, finish it, commit it. Nothing in it is discarded.
- Standing laws: personal-tool wording (no explainer copy), drawers not popups, ramped motion, monochromatic with semantic status color only, no em dashes in UI copy, label caps (job names 24 characters, task labels 40), rebuild discipline, pathspec commits for planner files.

## Whole-file rules

- Read DESIGN.md (the index) and only the `design/` area files your files touch before UI work; reuse the closest existing element; a new element gets appended to its area file.
- Verification: the runner's own verify stage is off for this round (it would run Fable). Each job verifies its done check with a fresh-context subagent running Sonnet 5 (`claude-sonnet-5`) at medium effort with complete instructions and its own done check; a reviewer that did not produce the work beats self-review. Keep judgment and the final synthesis yourself.
- Ensure `.gitignore` excludes `.env` before any `git add`. Commit each job, push at job end, check items off in this file in the same commit. Trust repo state over the phase prompt when they disagree.
- Secrets: `~/.codex/auth.json` holds tokens. Read it for the email and plan only; never print, log, commit or echo any token or key value, in code, tests, transcripts or summaries.
- Progress honesty: claims check against tool results; UI verification via agent-browser DOM and snapshot on dev; confirm visible changes at a phone viewport.
- Keep each job under 5 concurrent subagents. On unrecoverable failure stop and state what blocks.
- You have ample context; do not stop, summarize, or suggest a new session on account of context limits.

---

## Job 1 — System tab wired, model and effort defaults, sticky effort. Verify: no

Goal: the half-built System tab actually governs the watchdog, and the app knows what model and effort a new planner or worker session starts with. Files: the uncommitted settings work (`server/modules/settings/*`, `src/components/settings/**`, `SystemSettingsTab.tsx`), `watchdog.service.ts` (policy reads, `bootFreshPlanner` and the handoff and rotation spawn paths), the settings store, DESIGN.md index and `design/settings.md`. Dependencies: none; runs first because it owns the uncommitted tree.

- [x] Land the work in progress: read the uncommitted changes, keep them all, and make them this job's first commit (DESIGN.md index plus `design/` files, System tab, settings service, routes and test) before any new edit.
- [x] Every System-tab switch gates the behavior it names: the watchdog reads the stored policy through the settings service for all ten switches (planner rotation, terminal wakes, liveness sweep, dispatch run liveness, resource alerts, weekly self-test, weekly maintenance, handoff automation, punch-list watching, recovery notices); the stored value always wins over a code default; switches that act on Willem's own sessions (rotation, handoff automation) default off. Fix the orphaned rotation key: the route writes `watchdog_planner_rotation` while the watchdog still reads `planner_rotation_enabled`; one key, migrated, read everywhere.
- [x] The rotation threshold is editable in the System tab and saved through the same round trip; the watchdog reads the stored value.
- [x] A Models section in the System tab: default model and default effort for new planner sessions (seed: Fable 5, medium) and for new direct worker sessions (seed: Fable 5, high), each row showing the provider mark (the Claude mark or the OpenAI mark from `src/components/llm-provider-logo/`, muted, icon-sized) next to the model name; models offered are the catalog's per provider; stored in the cross-device settings store like everything else.
- [x] Sticky model and effort on every planner spawn: boot, handoff and rotation carry the previous planner row's model and effort into the new session (the SDK options receive both; today no effort is ever passed); the Models default applies only when the project has no previous planner row. The same rule for direct worker spawns where the app spawns them.

Done check: on dev: flipping each System switch changes the watchdog's behavior for a stubbed event of that kind (log the policy read); the rotation key round-trips and the threshold edit persists across two browser profiles; the Models rows save and reload; a stub planner row with effort xhigh spawns its successor with xhigh (read the spawn options from the log), and a project with no planner row spawns with the Models default. Fresh-context Sonnet 5 verification. Commit.

## Job 2 — Dispatch runner: per-unit engines, Luna verify, Codex jobs linked, limits, guards. Verify: no

Goal: a chain can mix engines per job, every job is verified by Luna, Codex jobs show up in the app like Claude jobs, a Codex usage limit waits instead of failing, and both engines share one widened destructive-command guard. Files: `scripts/macos/dispatch-chain-runner`, `scripts/macos/dispatch`, `server/modules/watchdog/watchdog.routes.ts` and `watchdog.service.ts` (session announce and registration), `sessions.db.ts`, the Codex session synchronizer, `~/.codex/config.toml`, `~/.codex/hooks.json`, the Claude PreToolUse guard in the user settings (`hooks/git-guard.js`), `design/worker-pane-and-jobs.md` if the jobs column needs a note. Dependencies: none.

- [x] Per-unit headers: a prompt file may carry `<!-- engine: claude|codex -->`, `<!-- model: <id> -->` and `<!-- effort: <level> -->` in its header; they override the chain's `DISPATCH_ENGINE`, `DISPATCH_MODEL` and `DISPATCH_EFFORT` for that unit only; a missing header falls back to the env, then the engine default (Claude: `claude-fable-5` at high with the `claude-opus-4-8` capacity fallback; Codex: `gpt-5.6-sol` at high). The announced session and the jobs column carry the unit's actual engine and model. `dispatch append` units get the same treatment.
- [x] Verify stage on Luna: the verify stage runs Codex `gpt-5.6-luna` at high regardless of the build unit's engine, reading its verdict from the Codex last-message file; `DISPATCH_VERIFY_ENGINE` and `DISPATCH_VERIFY_MODEL` env overrides exist only so stub chains and tests can pin it (the stub recipe with a `claude` shim on PATH must still run end to end).
- [x] Codex jobs link to their rollout: the runner captures the Codex thread id for each Codex unit (from `codex exec --json` events or whatever `codex exec` exposes; confirm against `codex exec --help` first), announces the session with `provider: codex` and that id, and the watchdog registers it so the row the Codex synchronizer indexes and the row the runner announces are one row: the worker pane renders the Codex transcript through the existing rollout parser, live during the job, and the jobs column shows the job like any Claude job. The announce route stops defaulting every dispatch session to Claude.
- [x] Codex usage limit waits: a Codex unit exiting on the usage-limit message family ("hit your usage limit" and its variants) is a limit, not a defect: journal one line, post a recovery notification (not a planner wake), wait until the `resets_at` of the newest rollout's `rate_limits` (a fixed backoff when none is readable), then re-run the unit on the uncommitted tree; the commit gate never trips on a limit. Never switch models or accounts automatically; a weekly cap with no reset inside 12 hours stops the chain with a decision-needed notification naming Terra as the manual fallback.
- [x] Subagents off for Codex: find the config key Codex 0.147 uses for subagents or multi-agent features (`codex features`, the config reference), set it off in `~/.codex/config.toml`, and have `run_codex` pass the same override explicitly so a config edit cannot turn it back on for dispatched units.
- [x] One widened guard for both engines: a single Node script (checked into `scripts/macos/`) that both the Claude PreToolUse hook and the Codex `pre_tool_use` hook call, blocking the existing destructive git set (force push in any spelling, hard reset, force clean, force branch delete, unpathed checkout or restore of the tree) plus recursive force deletes in any flag order (the remove command with recursive and force flags together, long or short form, with or without sudo), `find` with `-delete` or an exec of remove, the git clean command with any flags, `DROP TABLE` and `DROP DATABASE` in any shell-invoked SQL, and writes, moves or deletes whose target path is outside the current project, `~/Projects/spoton-worker`, `~/forge-logs` and `/tmp` (redirections included); shell chains are split and each segment checked. A test file of commands that must block and commands that must pass runs under `node --test`. The Codex hook is re-trusted through Codex's own trust flow (update the trusted hash the way Codex computes it and prove the hook fires without a trust prompt); never bypass hook trust.

Done check: on dev (`DISPATCH_SERVER_URL=http://127.0.0.1:4748`, `DISPATCH_DB_PATH=~/.cloudcli-dev/auth.db`): a two-unit chain in `~/Projects/codex-smoke` where unit 1 is `<!-- engine: codex -->` doing one real tiny edit and commit and unit 2 is a Claude-shim stub unit runs to completion; the Luna verify stage runs for both and its verdicts land in the journal; the jobs column on dev shows both units with the right engine and model, and the worker pane renders the Codex unit's transcript; a stub `codex` on PATH that prints the limit message and exits 1 makes the runner journal a wait and retry rather than fail; the guard test passes and a manual `codex exec` and `claude -p` each get denied on a recursive force delete under /tmp and on a force push without a trust prompt; `zsh -n` passes on both scripts. Fresh-context Sonnet 5 verification. Commit.

## Job 3 — ChatGPT in the account switcher, with real usage. Verify: no

Goal: the account switcher shows the ChatGPT login next to the Claude accounts with the same kind of usage meters, read from Codex's own rollout files. Files: `accounts.service.ts`, `accounts.routes.ts`, `codex-auth.provider.ts`, `provider-token-usage.service.ts`, the Codex session synchronizer (event hook for new rollout events), `AccountsPanel.tsx` and `SidebarFooter.tsx`, `design/sidebar.md`. Dependencies: none.

- [ ] A ChatGPT group in the account switcher below the Claude accounts: the OpenAI mark, the login email (decoded from the id token, never the token itself), the plan name, and 5-hour and 7-day meters using the existing meter bars, read from the newest `token_count` event's `rate_limits` (`used_percent`, `resets_at`, window) across `~/.codex/sessions`; the countdown and reset clock in the same format as the Claude meters. No switch, enable, disable or park controls (one login). Logged out or token stale: the row says so plainly with the one-line fix ("codex login on the mini"), no meter.
- [ ] Freshness is never silent: an "updated Xs ago" hint in the muted meta style under the ChatGPT meters; "no usage recorded yet" when no rollout carries a reading.
- [ ] Live refresh: the reading updates on drawer open and whenever the Codex session synchronizer sees a new `token_count` event, pushed over the existing WebSocket; nothing polls while nothing is watching.
- [ ] `/api/accounts` carries the ChatGPT entry in a shape the panel renders without special-casing beyond "no controls"; phone layout holds.

Done check: on dev: the switcher shows the ChatGPT group with the email, plan and two meters matching the newest rollout's `rate_limits` values (compare against a jq read of the file); a new rollout event written by a real `codex exec` one-liner updates the meters without reload; the logged-out rendering is exercised with a stubbed auth read; no token value appears in any log, response or test fixture (grep the transcript-visible output). Fresh-context Sonnet 5 verification. Commit.

## Job 4 — Model switcher trimmed and marked, planner and worker sessions on Codex. Verify: no

Goal: the switcher offers the models we actually use, with a subtle provider mark, and a planner or direct worker session can run on Codex end to end. Files: `codex-models.provider.ts`, `useChatProviderState.ts`, `providerEffort.ts`, `ComposerModelMenu.tsx`, `agent.routes.ts` (the doc comment), `watchdog.service.ts` (`bootFreshPlanner`, handoff and rotation spawns, direct worker spawn), the Codex runtime provider, `design/composer.md`. Dependencies: job 1 (sticky model and effort).

- [ ] Codex catalog: exactly `gpt-5.6-sol` (GPT-5.6 Sol), `gpt-5.6-terra` (GPT-5.6 Terra) and `gpt-5.6-luna` (GPT-5.6 Luna); efforts low, medium, high, xhigh; default effort high for all three; default model Sol. The client's pre-hydration fallback becomes `gpt-5.6-sol`; the OpenAPI comment naming `gpt-5.4` is corrected; no "5.4" or "5.5" remains anywhere in the repo's model surface.
- [ ] Provider marks, subtle: the composer model menu shows the Claude mark and the OpenAI mark as the header of each provider's model group, and the switcher's trigger shows the current model's mark at icon size beside the name; marks render in currentColor at the row's muted ink so every theme holds; effort labels read the same across providers ("High", "Extra"). Nowhere else gains a mark in this job.
- [ ] Planner sessions on Codex: boot, handoff and rotation spawn the successor on the previous planner row's provider as well as its model and effort (job 1); when the provider is Codex, the boot message is the body of the planner command file (`~/.claude/commands/planner.md`) sent inline, since Codex has no slash commands, and the same for the worker command on a direct Codex worker session; the spawned Codex session is registered with `provider: codex` so its transcript renders through the rollout parser.

Done check: on dev: the menu lists only the three 5.6 models with high preselected and marks visible (DOM); in the `~/Projects/codex-smoke` project, switch a planner chat to GPT-5.6 Sol, trigger the handoff spawn, and the successor boots on Codex, posts an opening, and renders in the planner pane (one real Codex boot, bounded); a direct worker session on Codex confirms boot with the worker command's one-line acknowledgment; phone viewport holds. Fresh-context Sonnet 5 verification. Commit.

## Job 5 — Jobs view truth: beam, titles, twins (appended 2026-08-27). Verify: no

Goal: what the sidebar and jobs column say matches what is running. Files: the project row border beam, session title derivation for dispatched and verify sessions (server side where the title is set from the prompt, and any client fallback), the jobs history query and its supersession logic in the watchdog and jobs API, `design/sidebar.md`, `design/worker-pane-and-jobs.md`. Dependencies: none.

- [ ] Border beam while a worker runs: the project row's beam is missing entirely while a dispatched job runs (observed on the Command Center row with chain codexint active); find why (activity detection, origin filter, or the beam gated on a state dispatch never sets) and fix so a running dispatched, direct or verify session lights the beam; regression check with a stubbed running dispatch row.
- [ ] Session titles from prompt files: a session spawned from a prompt file is titled by the file's `<!-- name: ... -->` header (verify sessions "Verify: <name>"), never by the raw prompt text; header comments are stripped from every prompt-derived title everywhere (worker pane header, run switcher, sidebar rows, jobs drawer); existing rows with a comment-shaped title are retitled on load.
- [ ] Twins hidden: a job in jobs history is hidden when another job of the same punch list file and job section (or one naming it in a `<!-- supersedes: -->` header) is running or completed, whichever chain ran first; failed, stopped and never-reached twins disappear, database rows stay; applies retroactively to ui11/ui11r, ui13/ui13r, ui15/ui15r/ui15r2 and codex/codexint. The jobs column and the history drawer read the same filtered list. This item replaces the supersession task in ui15 job 0; that task is dropped at the ui15 rerun compile.

Done check: on dev with a stub running dispatch row: the project row shows the beam (DOM); a session created from a prompt file with header comments titles as the header name and a verify session as "Verify: <name>"; with the live DB's chain rows copied to dev (named-columns recipe), the jobs column shows one entry per punch list job for the ui15 family and the codex family, hidden-row counts logged, no rows deleted. Fresh-context Sonnet 5 verification. Commit.
