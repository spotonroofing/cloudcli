# PUNCHLIST_codex — Codex and ChatGPT as a peer of Claude in Command Center

## Goal

Willem upgraded the mini's ChatGPT login to Pro on 2026-08-27. Codex CLI (GPT-5.6 Sol builds, Luna verifies, Terra as the weekly-cap fallback) becomes a first-class engine next to Claude everywhere the app and the harness touch models: settings hold per-role model and effort defaults, planner sessions carry model and effort forward and can run on Codex, the dispatch runner picks engine and model per job and verifies on Luna, Codex jobs show up in the worker pane and jobs column like Claude jobs, the ChatGPT login sits in the account switcher with real usage, and the model switcher shows the three 5.6 models under a subtle provider mark. Dev-first, end-to-end verification, promote after Willem's look.

## Stack and decisions already made

- Worker seat (2026-08-27): Codex Sol at high (xhigh for hard jobs, standard speed, subagents off) owns backend, wiring and harness jobs; Opus 5 at high owns UI jobs; verify stage is Codex Luna, never Fable and never the build model; Fable 5 is the planner only, medium effort by default and sticky to Willem's last pick. This chain itself runs on Fable by Willem's explicit ask.
- The server model catalog already carries gpt-5.6-sol, terra and luna with per-model effort blocks (server/modules/providers/list/codex/codex-models.provider.ts); the stale "GPT-5.4" is the client placeholder in src/components/chat/hooks/useChatProviderState.ts and an OpenAPI comment in server/modules/agent/agent.routes.ts. Codex is a working interactive provider (SDK based, resume and abort supported) with a rollout parser (codex-sessions.provider.ts) and a rollout indexer (codex-session-synchronizer).
- ChatGPT usage has exactly one local source: the `rate_limits` block on `token_count` events in rollouts under ~/.codex/sessions (primary = 5 hour, secondary = 7 day, plus plan_type); server/modules/providers/services/provider-token-usage.service.ts already parses the sibling total_token_usage. Never invent a percentage.
- Accounts (2026-08-27): nothing automated enables, disables or logs into an account. Watchdog: anything acting on Willem's own sessions defaults off and only he turns it on.
- Standing laws: personal-tool wording, drawers not popups, ramped motion, monochromatic icons with semantic status color only, DESIGN.md consistency (design/ area files), mobile parity, no em dashes in UI copy, label caps, rebuild discipline, pathspec commits.

## Whole-file rules

- Read DESIGN.md (the index) and only the design/ area files your job touches before UI work; reuse the closest existing element; new elements get appended to their area file.
- Ensure `.gitignore` excludes `.env` before any `git add`. Commit each job, push at job end, check items off in this file in the same commit. Trust repo state over the phase prompt when they disagree.
- The runner's verify stage is off for this chain; each job runs its own fresh-context verifier as a subagent on claude-sonnet-5 at medium effort against the job's done check before committing. Keep each job under 5 concurrent Fable subagents.
- Progress honesty: claims check against tool results; UI verification via agent-browser DOM and snapshot on dev (4748); confirm visible changes at a phone viewport.
- Dev instance: port 4748, config dir ~/.claude-dev, DB ~/.cloudcli-dev/auth.db. Smoke chains run against dev with DISPATCH_SERVER_URL=http://127.0.0.1:4748 and DISPATCH_DB_PATH=~/.cloudcli-dev/auth.db, and against a scratch repo path so the dev watchdog's chain-end wake drops instead of waking a real planner.
- Never print a token, key or credential to output; use it, never display it.
- On unrecoverable failure stop and state what blocks. You have ample context; do not stop, summarize, or suggest a new session on account of context limits.

---

## Job 1 — Settings wired, per-role model defaults, sticky planner effort. Verify: yes

Goal: the System tab actually governs the watchdog, settings hold the model and effort defaults for every role, and a new planner session inherits the last model and effort instead of a code constant. Files: server/modules/settings/*, src/components/settings/view/tabs/SystemSettingsTab.tsx and the settings tab types, server/modules/watchdog/watchdog.service.ts (policy reads and bootFreshPlanner), src/components/chat/hooks/useChatProviderState.ts, the cloud settings store, server/modules/agent/agent.routes.ts, DESIGN.md index and design/settings.md. Dependencies: none; runs first because it owns the uncommitted tree.

- [ ] Opening commit: the tree carries uncommitted ui15 job 0 work (DESIGN.md turned into an index plus design/ area files, the CLAUDE.md pointer, the System tab with its routes, service, test, and PlannerRotationSection removed). Commit it as-is first with the message "feat(settings): System tab and DESIGN.md split (ui15 job 0 WIP)", then build on it.
- [ ] The watchdog reads the stored policy for all ten System switches; nothing reads a code default when a stored setting exists. The planner-rotation switch keys on the new watchdog_planner_rotation key and the old planner_rotation_enabled key is retired everywhere. The rotation threshold is an editable field that saves. Switches that act on Willem's sessions (rotation, spawning, handoff automation) default off.
- [ ] A Models section (in the System tab, or its own Models tab if that reads cleaner; state the choice) with per-role defaults, each a provider-aware model picker with an effort picker beside it: Planner sessions (default Claude Fable 5, medium), Direct worker sessions (default Codex GPT-5.6 Sol, high), Dispatched build (Codex GPT-5.6 Sol, high), Dispatched verify (Codex GPT-5.6 Luna, high). Stored in the settings store like everything else and readable through one GET endpoint the dispatch CLI can consult (job 2 wires that side).
- [ ] Sticky model and effort for planner sessions: boot, handoff spawn and rotation spawn carry both model and effort from the previous planner row (the row already records switcher changes), falling back to the Planner default from the Models section, never a code constant. A planner whose switcher was set to a Codex model spawns its successor on the Codex provider: the boot path takes the provider from the model's catalog entry instead of hardwiring claude, and the boot ritual runs on either engine (Codex reads AGENTS.md; if the /planner slash command cannot run on Codex, boot sends the command file's content as the first message and the summary says so).
- [ ] Client placeholder before the catalog hydrates: the codex fallback becomes gpt-5.6-sol at high; fix the OpenAPI comment that still names gpt-5.4.

Done check: on dev: flipping each System switch changes the watchdog's behavior for a stubbed event (name the policy read that gated each); the threshold edit persists across reload and in a second browser profile; the Models section round-trips every role through the store and the GET endpoint; a stub previous-planner row on claude-fable-5 at xhigh spawns a fresh planner at xhigh (read the new row); a stub row on gpt-5.6-sol spawns a Codex-provider planner that completes one short real turn on dev. Fresh-context verifier on Sonnet 5. Commit.

## Job 2 — Dispatch runner: per-job engine, Luna verify, Codex linking and limits, guardrails. Verify: yes

Goal: every dispatched job runs on the engine and model the planner names in its header, is verified by Luna, shows up in the app like a Claude job, rides through a Codex limit, and cannot destroy anything on either engine. Files: scripts/macos/dispatch, scripts/macos/dispatch-chain-runner, server/modules/watchdog/watchdog.routes.ts and watchdog.service.ts, server/modules/database/repositories/sessions.db.ts, the codex session synchronizer, ~/.codex/config.toml, ~/.codex/hooks.json, the Claude PreToolUse git guard hook (find it in the ~/.claude settings), CLAUDE.md. Dependencies: job 1 (the GET endpoint for dispatch defaults).

- [ ] Per-unit headers: `<!-- engine: claude|codex -->`, `<!-- model: <id> -->`, `<!-- effort: <level> -->` in a prompt file override the chain for that unit. Precedence: unit header, then DISPATCH_ENGINE, DISPATCH_MODEL and DISPATCH_EFFORT env, then the settings-store dispatch defaults read once at fire time, then the engine's built-in default. `dispatch` prints the resolved engine, model and effort per unit at fire time, and the manifest carries them so the jobs column can show the model on each job.
- [ ] Verify stage with its own engine and model: default Codex GPT-5.6 Luna at high from the settings defaults regardless of the build engine; `<!-- verify-model: <id> -->` per unit and DISPATCH_VERIFY_MODEL env override it. The verify stage never silently inherits the build model.
- [ ] Codex session linking: a Codex unit announces provider codex and its real rollout id (capture it from codex exec output, or the newest rollout under ~/.codex/sessions created after launch, whichever is reliable; state which) so the session row points at the rollout, the worker pane shows the Codex transcript through the existing rollout parser live and after reload, and the jobs column shows the job exactly like a Claude one. The separately indexed sidebar twin is reconciled to one row per job. Verify sessions link the same way.
- [ ] Codex limit handling: the "hit your usage limit" wording family on a Codex unit exit is a limit, not a failure: journal it, send a recovery notice (no planner wake), wait until the reset time read from the newest rollout's rate_limits, then retry the unit; the commit gate does not trip. No automatic model change; Terra is Willem's call.
- [ ] Codex config: subagents off in ~/.codex/config.toml (the real key per the codex docs or help), reasoning effort default high; a custom prompt at ~/.codex/prompts/worker.md carrying the same seven rules as ~/.claude/commands/worker.md so a direct Codex session boots with /prompts:worker the same way.
- [ ] Destructive-command guard widened on both engines (the Codex pre_tool_use hook and the Claude PreToolUse hook): recursive force deletes in any flag order (rm -rf, rm -fr, rm -r -f, long flags), find with -delete or -exec rm, git clean with any flags, DROP TABLE and DROP DATABASE, and rm, mv or shell redirects targeting paths outside the repo, ~/forge-logs, the memory repo and /tmp. Existing git rules stay. Re-trust the Codex hook the proper way, never with a bypass flag. Unit tests for the matcher cover each pattern and a benign sibling of each.
- [ ] CLAUDE.md gains one line each for the per-unit headers, the Luna verify default, and the Codex rollout link.

Done check: `zsh -n` passes on both scripts; matcher tests pass; a two-unit Codex smoke chain on dev against a scratch repo path, unit 1 on Sol and unit 2 with a header pinning claude-sonnet-5 at low, both verified by Luna: the dev jobs column shows each unit with its model, the worker pane shows the Codex rollout live, each verify verdict comes from Luna's last-message file, the journal carries both; a simulated limit exit on a stub unit journals, notifies and retries without tripping the gate. Fresh-context verifier on Sonnet 5. Commit.

## Job 3 — Accounts: the ChatGPT login in the switcher with real usage. Verify: yes

Goal: the account switcher shows the ChatGPT login next to the Claude accounts with the same meters, fed by the only real source. Files: server/modules/accounts/accounts.service.ts and accounts.routes.ts, src/components/sidebar/.../AccountsPanel.tsx and SidebarFooter.tsx, server/modules/providers/list/codex/codex-auth.provider.ts, provider-token-usage.service.ts, the codex session synchronizer (rollout watch), design/sidebar.md. Dependencies: none.

- [ ] A ChatGPT group under the Claude accounts in the switcher: the login's email (the existing id-token decoder), plan type, and the same meter anatomy as a Claude account: 5-hour and 7-day bars with percent, reset countdown and clock, read from the newest token_count event's rate_limits across ~/.codex/sessions; "no usage yet" when no rollout exists. A small muted provider mark on each group header (Claude mark, OpenAI mark; the marks in src/components/llm-provider-logo).
- [ ] Freshness: when the rollout indexer sees a new token_count event, the meters update over the existing WebSocket without reload; an "updated Xs ago" hint in the muted meta style on the ChatGPT group (ui15 job 7 brings the same to Claude accounts; do not build the Claude side here).
- [ ] Login state: a missing or stale ChatGPT token renders "signed out" with the plain line "run codex login on the mini"; no login automation.
- [ ] API parity: GET /api/accounts includes the ChatGPT entry (provider, email, plan, usage, freshness); switch, enable and disable answer with a clear not-applicable for it (one login, no rotation).

Done check: on dev: the switcher renders the ChatGPT group with percentages equal to the newest rollout's rate_limits (compare the numbers); one short Codex turn on dev updates the meters without reload; the API shape is verified with curl; the signed-out state renders with a stubbed auth file. Phone holds. Fresh-context verifier on Sonnet 5. Commit.

## Job 4 — Model switcher and provider marks. Verify: no

Goal: the switcher lists exactly the three GPT-5.6 models with the right efforts, and a subtle mark tells the engine at a glance in the switcher and settings, nowhere else. Files: src/components/chat/view/.../ComposerModelMenu.tsx, codex-models.provider.ts, the Claude models provider, src/components/llm-provider-logo/*, the Settings Models section from job 1, design/composer.md, design/settings.md. Dependencies: job 1 (Models section).

- [ ] Codex catalog trimmed to Sol (default), Terra and Luna; efforts offered low, medium, high, xhigh with high the default on all three; the 5.5, 5.4 and 5.4 Mini entries go.
- [ ] Provider marks in the model switcher: each provider's group headed by its mark, and the closed switcher label carries a small muted mark before the model name; monochrome ink, one shared component and size scale, no marks in transcript rows or anywhere else new.
- [ ] The Settings Models pickers and the account switcher groups use the same mark component at the same scale.
- [ ] Labels read the same across providers ("GPT-5.6 Sol" beside its effort exactly as Claude models render theirs); the word ChatGPT appears only on the account group.

Done check: on dev: the switcher lists exactly Sol, Terra, Luna under the OpenAI mark with high preselected; the closed label shows the mark plus model and effort; the pre-hydration placeholder reads GPT-5.6 Sol; Settings pickers carry marks. Render-sanity pass (DOM). Phone holds. Commit.
